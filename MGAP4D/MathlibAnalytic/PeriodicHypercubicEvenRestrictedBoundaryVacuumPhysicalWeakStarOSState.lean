import MGAP4D.MathlibAnalytic.PeriodicHypercubicEvenRestrictedBoundaryVacuumPhysicalObservableAlgebra
import MGAP4D.MathlibAnalytic.PhysicalYangMillsGaugeInvariantOSBilinearForm

namespace MGAP4D
namespace MathlibAnalytic

open Filter MeasureTheory

noncomputable section

local instance restrictedBoundaryVacuumPhysicalWeakStarOSStateNeZero (H : ℕ) :
    NeZero (PeriodicHypercubicEvenSideLength H) := ⟨by
  simp [PeriodicHypercubicEvenSideLength]⟩

local instance restrictedBoundaryVacuumPhysicalWeakStarOSStateTopologicalGroup (N : ℕ) :
    IsTopologicalGroup (Matrix.specialUnitaryGroup (Fin N) ℂ) :=
  specialUnitaryGroupIsTopologicalGroup N

local instance restrictedBoundaryVacuumPhysicalWeakStarOSStateCompactSpace (N : ℕ) :
    CompactSpace (Matrix.specialUnitaryGroup (Fin N) ℂ) :=
  specialUnitaryGroupCompactSpace N

local instance restrictedBoundaryVacuumPhysicalWeakStarOSStateSecondCountable (N : ℕ) :
    SecondCountableTopology (Matrix.specialUnitaryGroup (Fin N) ℂ) :=
  specialUnitaryGroupSecondCountableTopology N

local instance restrictedBoundaryVacuumPhysicalWeakStarOSStateMeasurableSpace (N : ℕ) :
    MeasurableSpace (Matrix.specialUnitaryGroup (Fin N) ℂ) :=
  specialUnitaryGroupMeasurableSpace N

local instance restrictedBoundaryVacuumPhysicalWeakStarOSStateBorelSpace (N : ℕ) :
    BorelSpace (Matrix.specialUnitaryGroup (Fin N) ℂ) :=
  specialUnitaryGroupBorelSpace N

/-- The finite effective-boundary expectation is literally the evaluation of
 the physical gauge-invariant weak-star state on the canonical scalar
 observable.

 This is the weak-star version of the already proved continuous-state same-root
 identity; no dual state or finite law is introduced independently. -/
theorem
    periodicHypercubicEvenRestrictedBoundaryVacuum_effectiveBoundary_expectation_eq_approximatingGaugeInvariantWeakStarState
    (H : ℕ → ℕ)
    (N : ℕ) (hN : 0 < N)
    [Nontrivial (Matrix.specialUnitaryGroup (Fin N) ℂ)]
    (beta : ℕ → ℝ) (hbeta : ∀ n, 0 ≤ beta n)
    (latticeSpacing : ℕ → ℝ)
    (latticeSpacing_pos : ∀ n, 0 < latticeSpacing n)
    (latticeSpacing_tendsto_zero :
      Tendsto latticeSpacing atTop (nhds 0))
    (physicalVolume : ℕ → ℝ)
    (physicalVolume_tendsto_atTop :
      Tendsto physicalVolume atTop atTop)
    (L : PhysicalFourDimensionalYangMillsProkhorovSubsequenceLimit
      (periodicHypercubicEvenRestrictedBoundaryVacuumPhysicalEmbedding
        H N hN beta hbeta
        latticeSpacing latticeSpacing_pos latticeSpacing_tendsto_zero
        physicalVolume physicalVolume_tendsto_atTop).toLatticeEmbedding)
    (n : ℕ)
    (O : BoundedContinuousFunction ℝ ℝ) :
    let S := periodicHypercubicEvenRestrictedBoundaryVacuumGaugeSymmetryLimit
      H N hN beta hbeta
      latticeSpacing latticeSpacing_pos latticeSpacing_tendsto_zero
      physicalVolume physicalVolume_tendsto_atTop L
    let Oinv :=
      periodicHypercubicEvenRestrictedBoundaryVacuumGaugeInvariantObservable
        H N hN beta hbeta
        latticeSpacing latticeSpacing_pos latticeSpacing_tendsto_zero
        physicalVolume physicalVolume_tendsto_atTop L O
    (∫ b,
      O (periodicHypercubicEvenBoundaryVacuumMoment
        (H (L.subsequence n)) N hN
        (beta (L.subsequence n)) (hbeta (L.subsequence n)) b)
      ∂(periodicHypercubicEvenBoundaryMarginalEffectiveMeasure
        (H (L.subsequence n)) N hN
        (beta (L.subsequence n)) (hbeta (L.subsequence n)))) =
      physicalYangMillsApproximatingGaugeInvariantWeakStarState S n Oinv := by
  dsimp only
  simpa using
    periodicHypercubicEvenRestrictedBoundaryVacuum_effectiveBoundary_expectation_eq_approximatingGaugeInvariantContinuousState
      H N hN beta hbeta
      latticeSpacing latticeSpacing_pos latticeSpacing_tendsto_zero
      physicalVolume physicalVolume_tendsto_atTop L n O

/-- The continuum weak-star state on a canonical scalar observable is exactly
 integration against the Prokhorov continuum law generated by the same Wilson
 Gibbs root. -/
theorem
    periodicHypercubicEvenRestrictedBoundaryVacuum_continuumGaugeInvariantWeakStarState_apply
    (H : ℕ → ℕ)
    (N : ℕ) (hN : 0 < N)
    [Nontrivial (Matrix.specialUnitaryGroup (Fin N) ℂ)]
    (beta : ℕ → ℝ) (hbeta : ∀ n, 0 ≤ beta n)
    (latticeSpacing : ℕ → ℝ)
    (latticeSpacing_pos : ∀ n, 0 < latticeSpacing n)
    (latticeSpacing_tendsto_zero :
      Tendsto latticeSpacing atTop (nhds 0))
    (physicalVolume : ℕ → ℝ)
    (physicalVolume_tendsto_atTop :
      Tendsto physicalVolume atTop atTop)
    (L : PhysicalFourDimensionalYangMillsProkhorovSubsequenceLimit
      (periodicHypercubicEvenRestrictedBoundaryVacuumPhysicalEmbedding
        H N hN beta hbeta
        latticeSpacing latticeSpacing_pos latticeSpacing_tendsto_zero
        physicalVolume physicalVolume_tendsto_atTop).toLatticeEmbedding)
    (O : BoundedContinuousFunction ℝ ℝ) :
    let S := periodicHypercubicEvenRestrictedBoundaryVacuumGaugeSymmetryLimit
      H N hN beta hbeta
      latticeSpacing latticeSpacing_pos latticeSpacing_tendsto_zero
      physicalVolume physicalVolume_tendsto_atTop L
    let Oinv :=
      periodicHypercubicEvenRestrictedBoundaryVacuumGaugeInvariantObservable
        H N hN beta hbeta
        latticeSpacing latticeSpacing_pos latticeSpacing_tendsto_zero
        physicalVolume physicalVolume_tendsto_atTop L O
    physicalYangMillsContinuumGaugeInvariantWeakStarState S Oinv =
      ∫ x, O x ∂ProbabilityMeasure.toMeasure L.continuumMeasure := by
  dsimp only
  simpa using
    periodicHypercubicEvenRestrictedBoundaryVacuum_continuumGaugeInvariantContinuousState_apply
      H N hN beta hbeta
      latticeSpacing latticeSpacing_pos latticeSpacing_tendsto_zero
      physicalVolume physicalVolume_tendsto_atTop L O

/-- Every physical OS quadratic observable on the scalar boundary-vacuum
 carrier has a literal finite effective-boundary expectation equal to its
 approximating weak-star state evaluation.

 The only identification used is subtype extensionality: on the identity gauge
 action the canonical scalar observable and `Theta(F) * F` have the same
 underlying bounded continuous function. -/
theorem
    periodicHypercubicEvenRestrictedBoundaryVacuum_osQuadratic_effectiveBoundary_expectation_eq_approximatingWeakStarState
    (H : ℕ → ℕ)
    (N : ℕ) (hN : 0 < N)
    [Nontrivial (Matrix.specialUnitaryGroup (Fin N) ℂ)]
    (beta : ℕ → ℝ) (hbeta : ∀ n, 0 ≤ beta n)
    (latticeSpacing : ℕ → ℝ)
    (latticeSpacing_pos : ∀ n, 0 < latticeSpacing n)
    (latticeSpacing_tendsto_zero :
      Tendsto latticeSpacing atTop (nhds 0))
    (physicalVolume : ℕ → ℝ)
    (physicalVolume_tendsto_atTop :
      Tendsto physicalVolume atTop atTop)
    (L : PhysicalFourDimensionalYangMillsProkhorovSubsequenceLimit
      (periodicHypercubicEvenRestrictedBoundaryVacuumPhysicalEmbedding
        H N hN beta hbeta
        latticeSpacing latticeSpacing_pos latticeSpacing_tendsto_zero
        physicalVolume physicalVolume_tendsto_atTop).toLatticeEmbedding)
    (D : PhysicalYangMillsGaugeInvariantOSReflectionData
      (periodicHypercubicEvenRestrictedBoundaryVacuumGaugeSymmetryLimit
        H N hN beta hbeta
        latticeSpacing latticeSpacing_pos latticeSpacing_tendsto_zero
        physicalVolume physicalVolume_tendsto_atTop L))
    (F : D.positiveTimeSubalgebra)
    (n : ℕ) :
    (∫ b,
      D.quadraticBoundedContinuousFunction F
        (periodicHypercubicEvenBoundaryVacuumMoment
          (H (L.subsequence n)) N hN
          (beta (L.subsequence n)) (hbeta (L.subsequence n)) b)
      ∂(periodicHypercubicEvenBoundaryMarginalEffectiveMeasure
        (H (L.subsequence n)) N hN
        (beta (L.subsequence n)) (hbeta (L.subsequence n)))) =
      physicalYangMillsApproximatingGaugeInvariantWeakStarState
        (periodicHypercubicEvenRestrictedBoundaryVacuumGaugeSymmetryLimit
          H N hN beta hbeta
          latticeSpacing latticeSpacing_pos latticeSpacing_tendsto_zero
          physicalVolume physicalVolume_tendsto_atTop L)
        n (D.quadraticObservable F) := by
  let O := D.quadraticBoundedContinuousFunction F
  have hOinv :
      periodicHypercubicEvenRestrictedBoundaryVacuumGaugeInvariantObservable
          H N hN beta hbeta
          latticeSpacing latticeSpacing_pos latticeSpacing_tendsto_zero
          physicalVolume physicalVolume_tendsto_atTop L O =
        D.quadraticObservable F := by
    apply Subtype.ext
    rfl
  rw [← hOinv]
  simpa [O] using
    periodicHypercubicEvenRestrictedBoundaryVacuum_effectiveBoundary_expectation_eq_approximatingGaugeInvariantWeakStarState
      H N hN beta hbeta
      latticeSpacing latticeSpacing_pos latticeSpacing_tendsto_zero
      physicalVolume physicalVolume_tendsto_atTop L n O

/-- The same-root finite effective-boundary OS quadratic expectations converge
 directly to the continuum weak-star OS quadratic state evaluation.

 This reaches the exact quantity consumed by the OS bilinear/Hilbert spine.
 The remaining model-dependent frontier is therefore only the finite
 positive-half reflected-observable pullback needed to inherit nonnegativity. -/
theorem
    periodicHypercubicEvenRestrictedBoundaryVacuum_osQuadratic_effectiveBoundary_expectation_tendsto_continuumWeakStarState
    (H : ℕ → ℕ)
    (N : ℕ) (hN : 0 < N)
    [Nontrivial (Matrix.specialUnitaryGroup (Fin N) ℂ)]
    (beta : ℕ → ℝ) (hbeta : ∀ n, 0 ≤ beta n)
    (latticeSpacing : ℕ → ℝ)
    (latticeSpacing_pos : ∀ n, 0 < latticeSpacing n)
    (latticeSpacing_tendsto_zero :
      Tendsto latticeSpacing atTop (nhds 0))
    (physicalVolume : ℕ → ℝ)
    (physicalVolume_tendsto_atTop :
      Tendsto physicalVolume atTop atTop)
    (L : PhysicalFourDimensionalYangMillsProkhorovSubsequenceLimit
      (periodicHypercubicEvenRestrictedBoundaryVacuumPhysicalEmbedding
        H N hN beta hbeta
        latticeSpacing latticeSpacing_pos latticeSpacing_tendsto_zero
        physicalVolume physicalVolume_tendsto_atTop).toLatticeEmbedding)
    (D : PhysicalYangMillsGaugeInvariantOSReflectionData
      (periodicHypercubicEvenRestrictedBoundaryVacuumGaugeSymmetryLimit
        H N hN beta hbeta
        latticeSpacing latticeSpacing_pos latticeSpacing_tendsto_zero
        physicalVolume physicalVolume_tendsto_atTop L))
    (F : D.positiveTimeSubalgebra) :
    Tendsto
      (fun n : ℕ =>
        ∫ b,
          D.quadraticBoundedContinuousFunction F
            (periodicHypercubicEvenBoundaryVacuumMoment
              (H (L.subsequence n)) N hN
              (beta (L.subsequence n)) (hbeta (L.subsequence n)) b)
          ∂(periodicHypercubicEvenBoundaryMarginalEffectiveMeasure
            (H (L.subsequence n)) N hN
            (beta (L.subsequence n)) (hbeta (L.subsequence n))))
      atTop
      (nhds
        (physicalYangMillsContinuumGaugeInvariantWeakStarState
          (periodicHypercubicEvenRestrictedBoundaryVacuumGaugeSymmetryLimit
            H N hN beta hbeta
            latticeSpacing latticeSpacing_pos latticeSpacing_tendsto_zero
            physicalVolume physicalVolume_tendsto_atTop L)
          (D.quadraticObservable F))) := by
  let S := periodicHypercubicEvenRestrictedBoundaryVacuumGaugeSymmetryLimit
    H N hN beta hbeta
    latticeSpacing latticeSpacing_pos latticeSpacing_tendsto_zero
    physicalVolume physicalVolume_tendsto_atTop L
  have hSequence :
      (fun n : ℕ =>
        ∫ b,
          D.quadraticBoundedContinuousFunction F
            (periodicHypercubicEvenBoundaryVacuumMoment
              (H (L.subsequence n)) N hN
              (beta (L.subsequence n)) (hbeta (L.subsequence n)) b)
          ∂(periodicHypercubicEvenBoundaryMarginalEffectiveMeasure
            (H (L.subsequence n)) N hN
            (beta (L.subsequence n)) (hbeta (L.subsequence n)))) =
      (fun n : ℕ =>
        physicalYangMillsApproximatingGaugeInvariantWeakStarState
          S n (D.quadraticObservable F)) := by
    funext n
    exact
      periodicHypercubicEvenRestrictedBoundaryVacuum_osQuadratic_effectiveBoundary_expectation_eq_approximatingWeakStarState
        H N hN beta hbeta
        latticeSpacing latticeSpacing_pos latticeSpacing_tendsto_zero
        physicalVolume physicalVolume_tendsto_atTop L D F n
  rw [hSequence]
  exact
    (tendsto_iff_forall_eval_tendsto_topDualPairing.mp
      (physical_yang_mills_gaugeInvariantWeakStarState_converges S))
      (D.quadraticObservable F)

end

end MathlibAnalytic
end MGAP4D
