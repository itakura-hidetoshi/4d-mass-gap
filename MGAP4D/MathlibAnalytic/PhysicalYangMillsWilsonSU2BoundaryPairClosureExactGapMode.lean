import MGAP4D.MathlibAnalytic.PhysicalYangMillsWilsonSU2BoundaryPairClosureCommonCarrierLimit
import MGAP4D.MathlibAnalytic.ExactGapClusterContractionRatio
import Mathlib.Tactic

namespace MGAP4D
namespace MathlibAnalytic

noncomputable section

open Filter MeasureTheory Set Topology
open scoped InnerProductSpace InnerProduct

/-- The public exact-gap one-step contraction ratio recovers exactly the
Hamiltonian/PVM/spectral exact-gap carrier after applying `-log`.

This theorem deliberately keeps the public provenance carrier
`exactGapValueReal`; it does not re-export a downstream numerical literal. -/
theorem neg_log_exactGapClusterContractionRatio :
    -Real.log exactGapClusterContractionRatio = exactGapValueReal := by
  unfold exactGapClusterContractionRatio
  rw [Real.log_exp]
  ring

private theorem su2BoundaryPairExactGapTwoRankPositive : 0 < (2 : ℕ) := by
  norm_num

local instance su2BoundaryPairExactGapSideLengthNeZero (H : ℕ) :
    NeZero (PeriodicHypercubicEvenSideLength H) := ⟨by
  simp [PeriodicHypercubicEvenSideLength]⟩

local instance su2BoundaryPairExactGapTopologicalGroup :
    IsTopologicalGroup (Matrix.specialUnitaryGroup (Fin 2) ℂ) :=
  specialUnitaryGroupIsTopologicalGroup 2

local instance su2BoundaryPairExactGapCompactSpace :
    CompactSpace (Matrix.specialUnitaryGroup (Fin 2) ℂ) :=
  specialUnitaryGroupCompactSpace 2

local instance su2BoundaryPairExactGapSecondCountable :
    SecondCountableTopology (Matrix.specialUnitaryGroup (Fin 2) ℂ) :=
  specialUnitaryGroupSecondCountableTopology 2

local instance su2BoundaryPairExactGapMeasurableSpace :
    MeasurableSpace (Matrix.specialUnitaryGroup (Fin 2) ℂ) :=
  specialUnitaryGroupMeasurableSpace 2

local instance su2BoundaryPairExactGapBorelSpace :
    BorelSpace (Matrix.specialUnitaryGroup (Fin 2) ℂ) :=
  specialUnitaryGroupBorelSpace 2

namespace PhysicalYangMillsGaugeInvariantOSReflectionData.OSPreHilbertData

variable {S : PhysicalFourDimensionalYangMillsSymmetryLimit}
variable {D : PhysicalYangMillsGaugeInvariantOSReflectionData S}
variable {P : D.OSPreHilbertData}

namespace StronglyContinuousPhysicalSemigroup

section SU2BoundaryPairExactGap

variable
    {halfExtent : ℕ → ℕ}
    {beta : ℕ → ℝ}
    {hbeta : ∀ n, 0 ≤ beta n}
    [Nontrivial (Matrix.specialUnitaryGroup (Fin 2) ℂ)]
    {Q : PhysicalYangMillsEvenPeriodicWilsonOSCoherentPositiveTimePullback
      S D halfExtent 2 su2BoundaryPairExactGapTwoRankPositive beta hbeta}
    {hInvariant : ∀ n,
      D.WeakStarReflectionInvariant
        (physicalYangMillsApproximatingGaugeInvariantWeakStarState S n)}
    {T : P.StronglyContinuousPhysicalSemigroup}
    {C : PhysicalYangMillsEvenPeriodicWilsonOSApproximatingSemigroupFamily
      S D halfExtent 2 su2BoundaryPairExactGapTwoRankPositive beta hbeta
        Q.toWeakStarBridge hInvariant}
    {G : PhysicalYangMillsEvenPeriodicWilsonOSApproximatingVacuumGapCertificate
      S D halfExtent 2 su2BoundaryPairExactGapTwoRankPositive beta hbeta
        Q.toWeakStarBridge hInvariant C}

/-- If the cutoff physical one-step eigenvalues converge to the public exact-gap
contraction ratio, then the continuum OS time-one operator has exactly that
one-step eigenvalue on the common-carrier mode.

Finite Wilson OS eigen-equations remain theorem-generated by the post-synthesis
boundary-pair closure route. -/
theorem physicalOperator_one_apply_exactGapClusterContractionRatio_of_normalizedPhysicalTransferModePositiveTimeBoundaryPairClosure
    (A : PhysicalYangMillsEvenPeriodicWilsonOSCommonCarrierGapTransfer
      S D halfExtent 2 su2BoundaryPairExactGapTwoRankPositive beta hbeta
        Q.toWeakStarBridge hInvariant P T C G)
    (psi : P.PhysicalHilbert)
    (f : (n : ℕ) →
      periodicHypercubicEvenSpecialUnitarySpatialSliceGaugeInvariantL2Submodule
        (halfExtent n) 2)
    (mu : ℕ → ℝ)
    (hmu : Tendsto mu atTop (𝓝 exactGapClusterContractionRatio))
    (hf : ∀ n,
      periodicHypercubicEvenSpecialUnitaryNormalizedPhysicalOneSlabTransferOperator
        (halfExtent n) 2 su2BoundaryPairExactGapTwoRankPositive
          (beta n) (hbeta n) (f n) = mu n • f n)
    (hBoundary : ∀ n,
      Q.physicalHilbertBoundaryMomentLinearIsometry hInvariant n
          (A.approximate n psi) =
        periodicHypercubicEvenSpecialUnitaryOneSidedBoundaryL2
          (halfExtent n) 2
          (periodicHypercubicEvenSpecialUnitaryPhysicalModeLp
            (halfExtent n) 2 (f n))
          (periodicHypercubicEvenSpecialUnitaryPhysicalTopModeLp
            (halfExtent n) 2 su2BoundaryPairExactGapTwoRankPositive
              (beta n) (hbeta n)))
    (hClosure : ∀ n,
      Q.PhysicalTransferModePositiveTimeSubmoduleBoundaryPairClosureAt
        hInvariant C n (f n)) :
    T.toPhysicalSemigroup.operator 1 psi =
      exactGapClusterContractionRatio • psi := by
  exact
    physicalOperator_one_apply_of_normalizedPhysicalTransferModePositiveTimeBoundaryPairClosure
      A psi f mu exactGapClusterContractionRatio hmu hf hBoundary hClosure

/-- Exact-gap specialization of the constructive SU(2) common-carrier mode
route.

When normalized physical one-slice transfer eigenvalues converge to
`exp (-exactGapValueReal)`, the resulting graph-closed vacuum-orthogonal OS
Hamiltonian mode has eigenvalue exactly `exactGapValueReal`.  The numerical
closed form remains upstream in the concrete Hamiltonian/PVM/spectral package;
this theorem uses only its public provenance carrier. -/
theorem exists_vacuumOrthogonalClosedRightHamiltonian_exactGapValueReal_mode_of_normalizedPhysicalTransferModePositiveTimeBoundaryPairClosure
    (A : PhysicalYangMillsEvenPeriodicWilsonOSCommonCarrierGapTransfer S D halfExtent 2 su2BoundaryPairExactGapTwoRankPositive beta hbeta Q.toWeakStarBridge hInvariant P T C G)
    (hInnerSymmetric : T.toPhysicalSemigroup.IsInnerSymmetric)
    (hHamiltonianSymmetric : T.closedRightHamiltonian.IsFormalAdjoint T.closedRightHamiltonian)
    (psi : P.VacuumOrthogonalHilbert)
    (f : (n : ℕ) → periodicHypercubicEvenSpecialUnitarySpatialSliceGaugeInvariantL2Submodule (halfExtent n) 2)
    (mu : ℕ → ℝ)
    (hmu : Tendsto mu atTop (𝓝 exactGapClusterContractionRatio))
    (hf : ∀ n, periodicHypercubicEvenSpecialUnitaryNormalizedPhysicalOneSlabTransferOperator (halfExtent n) 2 su2BoundaryPairExactGapTwoRankPositive (beta n) (hbeta n) (f n) = mu n • f n)
    (hBoundary : ∀ n, Q.physicalHilbertBoundaryMomentLinearIsometry hInvariant n (A.approximate n (psi : P.PhysicalHilbert)) = periodicHypercubicEvenSpecialUnitaryOneSidedBoundaryL2 (halfExtent n) 2 (periodicHypercubicEvenSpecialUnitaryPhysicalModeLp (halfExtent n) 2 (f n)) (periodicHypercubicEvenSpecialUnitaryPhysicalTopModeLp (halfExtent n) 2 su2BoundaryPairExactGapTwoRankPositive (beta n) (hbeta n)))
    (hClosure : ∀ n, Q.PhysicalTransferModePositiveTimeSubmoduleBoundaryPairClosureAt hInvariant C n (f n)) :
    ∃ z : (T.vacuumOrthogonalClosedRightHamiltonian hHamiltonianSymmetric).domain,
      (z : P.VacuumOrthogonalHilbert) = psi ∧
        T.vacuumOrthogonalClosedRightHamiltonian hHamiltonianSymmetric z =
          exactGapValueReal • psi := by
  obtain ⟨z, hz, hEigen⟩ :=
    exists_vacuumOrthogonalClosedRightHamiltonian_mode_of_normalizedPhysicalTransferModePositiveTimeBoundaryPairClosure
      A hInnerSymmetric hHamiltonianSymmetric psi f mu
        exactGapClusterContractionRatio exact_gap_cluster_contraction_ratio_pos hmu
        hf hBoundary hClosure
  refine ⟨z, hz, ?_⟩
  rw [neg_log_exactGapClusterContractionRatio] at hEigen
  exact hEigen

end SU2BoundaryPairExactGap

end StronglyContinuousPhysicalSemigroup

end PhysicalYangMillsGaugeInvariantOSReflectionData.OSPreHilbertData

end

end MathlibAnalytic
end MGAP4D
