import MGAP4D.MathlibAnalytic.FiniteWilsonGibbsSingleSourceVacuumPoincareOSLimitAssembly
import MGAP4D.MathlibAnalytic.FiniteWilsonOSAutomaticExactGapSingleLinkHeatBathTransferOrbitContraction

namespace MGAP4D
namespace MathlibAnalytic

open MeasureTheory

noncomputable section

/-- Complete continuum OS assembly whose finite-volume mass gap is generated
from the concrete single-link Wilson heat-bath Poincare inequality. -/
structure FiniteWilsonOSAutomaticSingleLinkHeatBathAnalyticLimitConstructionData
    (W : FiniteWilsonOSAutomaticApproximationFamily)
    (F : EuclideanYangMillsProjectiveCylinderFamily)
    (L : EuclideanYangMillsProjectiveLimitMeasure F) where
  gaugeGroup : Type
  [gaugeGroupGroup : Group gaugeGroup]
  [gaugeGroupTopology : TopologicalSpace gaugeGroup]
  [gaugeGroupCompact : CompactSpace gaugeGroup]
  [gaugeGroupNontrivial : Nontrivial gaugeGroup]
  [gaugeAction : MulAction gaugeGroup F.Configuration]
  gaugeActionMeasurable :
    ∀ g : gaugeGroup, Measurable (fun A : F.Configuration => g • A)
  gaugeInvariant :
    ∀ g : gaugeGroup,
      L.continuumMeasure.map (fun A : F.Configuration => g • A) =
        L.continuumMeasure
  fieldAlgebra : Type
  schwingerFunctions : ℕ → Type
  symmetric : Prop
  symmetric_proof : symmetric
  reflectionLimit : FiniteWilsonOSAutomaticReflectionLimitData W
  euclideanLimit : FiniteWilsonOSAutomaticEuclideanLimitData W
  clusterHeatBath :
    FiniteWilsonOSAutomaticExactGapSingleLinkHeatBathTransferOrbitContractionData W
  regularityLimit : FiniteWilsonOSAutomaticRegularityLimitData W

attribute [instance]
  FiniteWilsonOSAutomaticSingleLinkHeatBathAnalyticLimitConstructionData.gaugeGroupGroup
  FiniteWilsonOSAutomaticSingleLinkHeatBathAnalyticLimitConstructionData.gaugeGroupTopology
  FiniteWilsonOSAutomaticSingleLinkHeatBathAnalyticLimitConstructionData.gaugeGroupCompact
  FiniteWilsonOSAutomaticSingleLinkHeatBathAnalyticLimitConstructionData.gaugeGroupNontrivial
  FiniteWilsonOSAutomaticSingleLinkHeatBathAnalyticLimitConstructionData.gaugeAction

/-- Forget only the concrete heat-bath origin and enter the established
vacuum-Poincare continuum assembly. -/
noncomputable def
    FiniteWilsonOSAutomaticSingleLinkHeatBathAnalyticLimitConstructionData.toVacuumPoincareAnalyticData
    {W : FiniteWilsonOSAutomaticApproximationFamily}
    {F : EuclideanYangMillsProjectiveCylinderFamily}
    {L : EuclideanYangMillsProjectiveLimitMeasure F}
    (D : FiniteWilsonOSAutomaticSingleLinkHeatBathAnalyticLimitConstructionData W F L) :
    FiniteWilsonOSAutomaticVacuumPoincareAnalyticLimitConstructionData W F L :=
  { gaugeGroup := D.gaugeGroup
    gaugeGroupGroup := D.gaugeGroupGroup
    gaugeGroupTopology := D.gaugeGroupTopology
    gaugeGroupCompact := D.gaugeGroupCompact
    gaugeGroupNontrivial := D.gaugeGroupNontrivial
    gaugeAction := D.gaugeAction
    gaugeActionMeasurable := D.gaugeActionMeasurable
    gaugeInvariant := D.gaugeInvariant
    fieldAlgebra := D.fieldAlgebra
    schwingerFunctions := D.schwingerFunctions
    symmetric := D.symmetric
    symmetric_proof := D.symmetric_proof
    reflectionLimit := D.reflectionLimit
    euclideanLimit := D.euclideanLimit
    clusterPoincare := D.clusterHeatBath.toVacuumPoincareData
    regularityLimit := D.regularityLimit }

/-- The automatic OS datum generated by the full heat-bath route. -/
noncomputable def
    FiniteWilsonOSAutomaticSingleLinkHeatBathAnalyticLimitConstructionData.toAutomaticOSData
    {W : FiniteWilsonOSAutomaticApproximationFamily}
    {F : EuclideanYangMillsProjectiveCylinderFamily}
    {L : EuclideanYangMillsProjectiveLimitMeasure F}
    (D : FiniteWilsonOSAutomaticSingleLinkHeatBathAnalyticLimitConstructionData W F L) :=
  D.toVacuumPoincareAnalyticData.toVacuumOrthogonalDerivedInvarianceAnalyticData.toCoerciveTransferOrbitAnalyticData.toConstructedTransferOrbitAnalyticData.toConstructedHamiltonianTransferAnalyticData.toHamiltonianEigenactionAnalyticData.toFiniteDimensionalHamiltonianAnalyticData.toOrthonormalEigenbasisAnalyticData.toFiniteSpectralAnalyticData.toPositiveRayleighAnalyticData.toSymmetricRayleighAnalyticData.toTransferAnalyticData.toAutomaticData

/-- Reflection positivity, Euclidean invariance, heat-bath-generated clustering,
and regularity hold simultaneously. -/
theorem finite_wilson_os_single_link_heat_bath_four_limit_properties
    {W : FiniteWilsonOSAutomaticApproximationFamily}
    {F : EuclideanYangMillsProjectiveCylinderFamily}
    {L : EuclideanYangMillsProjectiveLimitMeasure F}
    (D : FiniteWilsonOSAutomaticSingleLinkHeatBathAnalyticLimitConstructionData W F L) :
    D.toAutomaticOSData.reflectionLimit.ContinuumReflectionPositive ∧
      D.toAutomaticOSData.euclideanLimit.toEuclideanInvarianceLimitData.ContinuumEuclideanInvariant ∧
      D.toAutomaticOSData.clusterLimit.toClusterLimitData.ContinuumClusterProperty ∧
      D.toAutomaticOSData.regularityLimit.toRegularityLimitData.ContinuumRegularity :=
  finite_wilson_os_vacuum_poincare_four_limit_properties
    D.toVacuumPoincareAnalyticData

/-- The continuum correlation inherits exact-gap decay from the concrete
single-link heat-bath Poincare estimate. -/
theorem finite_wilson_os_single_link_heat_bath_continuum_bound
    {W : FiniteWilsonOSAutomaticApproximationFamily}
    {F : EuclideanYangMillsProjectiveCylinderFamily}
    {L : EuclideanYangMillsProjectiveLimitMeasure F}
    (D : FiniteWilsonOSAutomaticSingleLinkHeatBathAnalyticLimitConstructionData W F L)
    (O : D.clusterHeatBath.Observable) (r : ℕ) :
    ‖D.clusterHeatBath.continuumConnectedCorrelation O r‖ ≤
      D.clusterHeatBath.decayAmplitude O *
        exactGapClusterContractionRatio ^ r :=
  finite_wilson_exact_gap_single_link_heat_bath_continuum_bound
    D.clusterHeatBath O r

/-- The heat-bath-generated continuum package is OS/Wightman-ready. -/
theorem finite_wilson_os_single_link_heat_bath_measure_package_ready
    {W : FiniteWilsonOSAutomaticApproximationFamily}
    {F : EuclideanYangMillsProjectiveCylinderFamily}
    {L : EuclideanYangMillsProjectiveLimitMeasure F}
    (D : FiniteWilsonOSAutomaticSingleLinkHeatBathAnalyticLimitConstructionData W F L) :
    D.toAutomaticOSData.toProjectiveLimitTransferData.toContinuumConstruction.toMeasurePackage.ready :=
  finite_wilson_os_vacuum_poincare_measure_package_ready
    D.toVacuumPoincareAnalyticData

variable {W : FiniteWilsonOSAutomaticApproximationFamily}
  (R : FiniteWilsonGibbsSingleSourceProjectiveRealization W)
  [∀ x, Fintype (R.fieldValue x)]
  [∀ x, Countable (R.fieldValue x)]
  [∀ x, DiscreteMeasurableSpace (R.fieldValue x)]

abbrev FiniteWilsonGibbsSingleSourceSingleLinkHeatBathOSLimitData :=
  FiniteWilsonOSAutomaticSingleLinkHeatBathAnalyticLimitConstructionData W
    R.toProjectiveRealization.toProjectiveCylinderFamily
    R.projectiveLimitMeasure

/-- The explicit common-source continuum law is OS/Wightman-ready under the
single-link heat-bath route. -/
theorem finite_wilson_single_source_single_link_heat_bath_os_limit_ready
    (D : FiniteWilsonGibbsSingleSourceSingleLinkHeatBathOSLimitData R) :
    (R.explicitOSContinuumConstruction
      (R.automaticOSLimitTransferData D.toAutomaticOSData)).toMeasurePackage.ready :=
  finite_wilson_single_source_vacuum_poincare_os_limit_ready
    R D.toVacuumPoincareAnalyticData

variable {κ : Type*} [Countable κ] [Nonempty κ]
  {β : κ → Type*} [∀ k, MeasurableSpace (β k)]
  [∀ k, StandardBorelSpace (β k)]

/-- All four continuum construction routes are OS/Wightman-ready under the
concrete single-link heat-bath route. -/
theorem finite_wilson_single_source_single_link_heat_bath_four_routes_ready
    (S : EuclideanYangMillsCountableSkeletonData
      R.toProjectiveRealization.toProjectiveCylinderFamily κ β)
    (D : FiniteWilsonGibbsSingleSourceSingleLinkHeatBathOSLimitData R) :
    (R.explicitOSContinuumConstruction
        (R.automaticOSLimitTransferData D.toAutomaticOSData)).toMeasurePackage.ready ∧
      (R.standardBorelOSContinuumConstruction
        (R.automaticOSLimitTransferData D.toAutomaticOSData)).toMeasurePackage.ready ∧
      (R.compactTightOSContinuumConstruction
        (R.automaticOSLimitTransferData D.toAutomaticOSData)).toMeasurePackage.ready ∧
      (R.countableSkeletonOSContinuumConstruction S
        (R.automaticOSLimitTransferData D.toAutomaticOSData)).toMeasurePackage.ready :=
  finite_wilson_single_source_vacuum_poincare_four_routes_ready
    R S D.toVacuumPoincareAnalyticData

/-- All four heat-bath-driven routes use exactly the explicit `globalObserve`
pushforward continuum law. -/
theorem finite_wilson_single_source_single_link_heat_bath_four_route_measures_eq_explicit
    (S : EuclideanYangMillsCountableSkeletonData
      R.toProjectiveRealization.toProjectiveCylinderFamily κ β)
    (D : FiniteWilsonGibbsSingleSourceSingleLinkHeatBathOSLimitData R) :
    (R.explicitOSContinuumConstruction
        (R.automaticOSLimitTransferData D.toAutomaticOSData)).limit.continuumMeasure = R.continuumMeasure ∧
      (R.standardBorelOSContinuumConstruction
        (R.automaticOSLimitTransferData D.toAutomaticOSData)).limit.continuumMeasure = R.continuumMeasure ∧
      (R.compactTightOSContinuumConstruction
        (R.automaticOSLimitTransferData D.toAutomaticOSData)).limit.continuumMeasure = R.continuumMeasure ∧
      (R.countableSkeletonOSContinuumConstruction S
        (R.automaticOSLimitTransferData D.toAutomaticOSData)).limit.continuumMeasure = R.continuumMeasure :=
  finite_wilson_single_source_vacuum_poincare_four_route_measures_eq_explicit
    R S D.toVacuumPoincareAnalyticData

end

end MathlibAnalytic
end MGAP4D
