# Two-sided confluent resolvent jet

This branch defines the Pascal-type two-node multiplicity recursion and its actual OS diagonal real-form strong-limit specialization. The closed `Nat.choose` coefficient formula is intentionally left for the next generic layer.
