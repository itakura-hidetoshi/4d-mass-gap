import MGAP4D.MathlibAnalytic.PhysicalYangMillsGaugeInvariantOSRealizableExact3320BoundaryDirichletEnergy
import MGAP4D.MathlibAnalytic.PhysicalYangMillsGaugeInvariantOSRealizableFloorGapTransfer
import MGAP4D.MathlibAnalytic.PhysicalYangMillsGaugeInvariantOSApproximatingCenteredQuadraticLimit
import MGAP4D.MathlibAnalytic.PhysicalYangMillsGaugeInvariantOSContinuumHalfQuadraticGapClosure
import MGAP4D.MathlibAnalytic.PhysicalYangMillsGaugeInvariantOSContinuumVacuum
import MGAP4D.MathlibAnalytic.PhysicalYangMillsGaugeInvariantOSExponentialGapSlope
import Mathlib.Tactic

noncomputable section

open Filter MeasureTheory Set Topology
open scoped InnerProductSpace

namespace MGAP4D
namespace MathlibAnalytic

local instance realizableFloorWeakStarQuadraticSideLengthNeZero (H : ℕ) :
    NeZero (PeriodicHypercubicEvenSideLength H) := ⟨by
  simp [PeriodicHypercubicEvenSideLength]⟩

local instance realizableFloorWeakStarQuadraticSpecialUnitaryIsTopologicalGroup (N : ℕ) :
    IsTopologicalGroup (Matrix.specialUnitaryGroup (Fin N) ℂ) :=
  specialUnitaryGroupIsTopologicalGroup N

local instance realizableFloorWeakStarQuadraticSpecialUnitaryCompactSpace (N : ℕ) :
    CompactSpace (Matrix.specialUnitaryGroup (Fin N) ℂ) :=
  specialUnitaryGroupCompactSpace N

local instance realizableFloorWeakStarQuadraticSpecialUnitarySecondCountableTopology (N : ℕ) :
    SecondCountableTopology (Matrix.specialUnitaryGroup (Fin N) ℂ) :=
  specialUnitaryGroupSecondCountableTopology N

local instance realizableFloorWeakStarQuadraticSpecialUnitaryMeasurableSpace (N : ℕ) :
    MeasurableSpace (Matrix.specialUnitaryGroup (Fin N) ℂ) :=
  specialUnitaryGroupMeasurableSpace N

local instance realizableFloorWeakStarQuadraticSpecialUnitaryBorelSpace (N : ℕ) :
    BorelSpace (Matrix.specialUnitaryGroup (Fin N) ℂ) :=
  specialUnitaryGroupBorelSpace N

/-- Quadratic decay factor selected at the realizable half-time
`k_n a_n ≈ t/2`.

The finite one-step norm factor is squared because an OS half-time norm square
produces the full-time quadratic decay. -/
def physicalYangMillsExact3320FloorHalfQuadraticFactor
    (S : PhysicalFourDimensionalYangMillsSymmetryLimit)
    (t : NNReal) (n : ℕ) : ℝ :=
  (physicalYangMillsExact3320FloorNormFactor S (t / 2) n) ^ 2

/-- The realizable half-time squared factor converges to the exact full-time
continuum quadratic factor `exp (-(33/20)t)`. -/
theorem physicalYangMillsExact3320FloorHalfQuadraticFactor_tendsto
    (S : PhysicalFourDimensionalYangMillsSymmetryLimit)
    (t : NNReal) :
    Tendsto
      (fun n : ℕ => physicalYangMillsExact3320FloorHalfQuadraticFactor S t n)
      atTop
      (nhds (Real.exp (-physicalYangMillsExact3320Mass * (t : ℝ)))) := by
  have h := physicalYangMillsExact3320FloorNormFactor_tendsto S (t / 2)
  have hsq := h.mul h
  convert hsq using 1
  · funext n
    simp only [physicalYangMillsExact3320FloorHalfQuadraticFactor, pow_two]
  · rw [← Real.exp_add]
    congr 1
    push_cast
    ring

/-- Scalar weak-star replacement for the former common-Hilbert-space floor
transfer.

No maps `H_∞ → H_n` and no isometric embeddings `H_n → H_∞` are postulated.
The finite exact `33/20` input is the literal compact-Haar boundary Poincaré
certificate from the geometric Dirichlet lane.  The only limiting datum retained
here is convergence of the **scalar centered OS quadratic value** along the
floor-selected realizable half-time trajectory.

The initial centered quadratic value needs no field: its convergence is already
theorem-generated by weak-star convergence of the actual finite Wilson states. -/
structure PhysicalYangMillsEvenPeriodicWilsonOSRealizableFloorWeakStarQuadraticGapTransfer
    (S : PhysicalFourDimensionalYangMillsSymmetryLimit)
    (D : PhysicalYangMillsGaugeInvariantOSReflectionData S)
    (halfExtent : ℕ → ℕ)
    (N : ℕ)
    (hN : 0 < N)
    [Nontrivial (Matrix.specialUnitaryGroup (Fin N) ℂ)]
    (beta : ℕ → ℝ)
    (hbeta : ∀ n, 0 ≤ beta n)
    (Q : PhysicalYangMillsEvenPeriodicWilsonOSCoherentPositiveTimePullback
      S D halfExtent N hN beta hbeta)
    (E : PhysicalFourDimensionalYangMillsContinuumEuclideanTimeTranslation S)
    (R : PhysicalYangMillsEvenPeriodicWilsonOSRealizablePositiveTemporalCovariance
      S D halfExtent N hN beta hbeta Q E)
    (hInvariant : ∀ n,
      D.WeakStarReflectionInvariant
        (physicalYangMillsApproximatingGaugeInvariantWeakStarState S n))
    (B : PhysicalYangMillsEvenPeriodicWilsonOSRealizableExact3320BoundaryDirichletGapCertificate
      S D halfExtent N hN beta hbeta Q E R hInvariant)
    (T :
      let Pinf :=
        physical_yang_mills_evenPeriodicWilsonOS_continuum_preHilbertData
          S D halfExtent N hN beta hbeta Q.toWeakStarBridge hInvariant
      Pinf.PositiveTimeObservableContractionSemigroup) where
  exchange : T.ReflectionTimeTranslationExchange
  evolved_floor_centered_quadratic_tendsto :
    ∀ (t : NNReal) (F : D.positiveTimeSubalgebra),
      let Pinf :=
        physical_yang_mills_evenPeriodicWilsonOS_continuum_preHilbertData
          S D halfExtent N hN beta hbeta Q.toWeakStarBridge hInvariant
      Tendsto
        (fun n : ℕ =>
          let Pn :=
            physical_yang_mills_evenPeriodicWilsonOS_approximating_preHilbertData
              S D halfExtent N hN beta hbeta Q.toWeakStarBridge hInvariant n
          Pn.osQuadraticValue
            (R.realizableCarrierTranslation hInvariant n
              (physicalTemporalFloorNatStep S.latticeSpacing (t / 2) n)
              (Pn.vacuumCenteredCarrier (Pn.carrierOfPositiveTime F))))
        atTop
        (nhds
          (Pinf.osQuadraticValue
            (T.carrierTranslation (t / 2)
              (Pinf.vacuumCenteredCarrier (Pinf.carrierOfPositiveTime F)))))

namespace PhysicalYangMillsEvenPeriodicWilsonOSRealizableFloorWeakStarQuadraticGapTransfer

variable
    {S : PhysicalFourDimensionalYangMillsSymmetryLimit}
    {D : PhysicalYangMillsGaugeInvariantOSReflectionData S}
    {halfExtent : ℕ → ℕ}
    {N : ℕ}
    {hN : 0 < N}
    [Nontrivial (Matrix.specialUnitaryGroup (Fin N) ℂ)]
    {beta : ℕ → ℝ}
    {hbeta : ∀ n, 0 ≤ beta n}
    {Q : PhysicalYangMillsEvenPeriodicWilsonOSCoherentPositiveTimePullback
      S D halfExtent N hN beta hbeta}
    {E : PhysicalFourDimensionalYangMillsContinuumEuclideanTimeTranslation S}
    {R : PhysicalYangMillsEvenPeriodicWilsonOSRealizablePositiveTemporalCovariance
      S D halfExtent N hN beta hbeta Q E}
    {hInvariant : ∀ n,
      D.WeakStarReflectionInvariant
        (physicalYangMillsApproximatingGaugeInvariantWeakStarState S n)}
    {B : PhysicalYangMillsEvenPeriodicWilsonOSRealizableExact3320BoundaryDirichletGapCertificate
      S D halfExtent N hN beta hbeta Q E R hInvariant}
    {T :
      let Pinf :=
        physical_yang_mills_evenPeriodicWilsonOS_continuum_preHilbertData
          S D halfExtent N hN beta hbeta Q.toWeakStarBridge hInvariant
      Pinf.PositiveTimeObservableContractionSemigroup}

/-- The finite boundary Poincaré certificate gives the exact floor-selected
half-time quadratic estimate without introducing a finite all-real-time
semigroup. -/
theorem finite_floor_half_quadratic_le
    (A : PhysicalYangMillsEvenPeriodicWilsonOSRealizableFloorWeakStarQuadraticGapTransfer
      S D halfExtent N hN beta hbeta Q E R hInvariant B T)
    (n : ℕ) (t : NNReal) (F : D.positiveTimeSubalgebra) :
    let Pn :=
      physical_yang_mills_evenPeriodicWilsonOS_approximating_preHilbertData
        S D halfExtent N hN beta hbeta Q.toWeakStarBridge hInvariant n
    Pn.osQuadraticValue
        (R.realizableCarrierTranslation hInvariant n
          (physicalTemporalFloorNatStep S.latticeSpacing (t / 2) n)
          (Pn.vacuumCenteredCarrier (Pn.carrierOfPositiveTime F))) ≤
      physicalYangMillsExact3320FloorHalfQuadraticFactor S t n *
        Pn.osQuadraticValue
          (Pn.vacuumCenteredCarrier (Pn.carrierOfPositiveTime F)) := by
  dsimp only
  let G := B.toOneStepExact3320GapCertificate
  have h := G.centered_osQuadraticValue_le_pow_sq
    n (physicalTemporalFloorNatStep S.latticeSpacing (t / 2) n)
    ((physical_yang_mills_evenPeriodicWilsonOS_approximating_preHilbertData
      S D halfExtent N hN beta hbeta Q.toWeakStarBridge hInvariant n).carrierOfPositiveTime F)
  dsimp only at h
  simpa only [physicalYangMillsExact3320FloorHalfQuadraticFactor,
    physicalYangMillsExact3320FloorNormFactor_eq_pow] using h

/-- The scalar weak-star floor limit generates the exact continuum half-time OS
quadratic decay certificate with mass `33/20`.

This is the key replacement for the previous common-Hilbert embedding route. -/
noncomputable def toHalfQuadraticGapCertificate
    (A : PhysicalYangMillsEvenPeriodicWilsonOSRealizableFloorWeakStarQuadraticGapTransfer
      S D halfExtent N hN beta hbeta Q E R hInvariant B T) :
    T.HalfQuadraticGapCertificate where
  mass := physicalYangMillsExact3320Mass
  mass_pos := physicalYangMillsExact3320Mass_pos
  quadraticDecayFactor := fun t =>
    Real.exp (-physicalYangMillsExact3320Mass * (t : ℝ))
  quadraticDecayFactor_nonneg :=
    exponential_quadraticDecayFactor_nonneg physicalYangMillsExact3320Mass
  slope_tendsto :=
    exponential_quadraticDecayFactor_slope_tendsto
      physicalYangMillsExact3320Mass
  exchange := A.exchange
  core_half_quadratic_decay := by
    intro t F
    let Pinf :=
      physical_yang_mills_evenPeriodicWilsonOS_continuum_preHilbertData
        S D halfExtent N hN beta hbeta Q.toWeakStarBridge hInvariant
    let Fpos := Pinf.positiveTimeElement F
    have hleft := A.evolved_floor_centered_quadratic_tendsto t Fpos
    have hinitial :=
      physical_yang_mills_evenPeriodicWilsonOS_centeredQuadraticValue_tendsto
        S D halfExtent N hN beta hbeta Q.toWeakStarBridge hInvariant Fpos
    have hfactor :=
      physicalYangMillsExact3320FloorHalfQuadraticFactor_tendsto S t
    have hright := hfactor.mul hinitial
    have hfinite : ∀ n : ℕ,
        let Pn :=
          physical_yang_mills_evenPeriodicWilsonOS_approximating_preHilbertData
            S D halfExtent N hN beta hbeta Q.toWeakStarBridge hInvariant n
        Pn.osQuadraticValue
            (R.realizableCarrierTranslation hInvariant n
              (physicalTemporalFloorNatStep S.latticeSpacing (t / 2) n)
              (Pn.vacuumCenteredCarrier (Pn.carrierOfPositiveTime Fpos))) ≤
          physicalYangMillsExact3320FloorHalfQuadraticFactor S t n *
            Pn.osQuadraticValue
              (Pn.vacuumCenteredCarrier (Pn.carrierOfPositiveTime Fpos)) := by
      intro n
      exact A.finite_floor_half_quadratic_le n t Fpos
    have hlimit := le_of_tendsto_of_tendsto hleft hright
      (Eventually.of_forall hfinite)
    simpa only [Fpos, Pinf.carrierOfPositiveTime_positiveTimeElement] using hlimit

/-- The continuum Wilson OS vacuum is normalized automatically; no normalization
field is retained in the weak-star floor transfer. -/
theorem continuum_isNormalized
    (A : PhysicalYangMillsEvenPeriodicWilsonOSRealizableFloorWeakStarQuadraticGapTransfer
      S D halfExtent N hN beta hbeta Q E R hInvariant B T) :
    (physical_yang_mills_evenPeriodicWilsonOS_continuum_preHilbertData
      S D halfExtent N hN beta hbeta Q.toWeakStarBridge hInvariant).IsNormalized :=
  physical_yang_mills_evenPeriodicWilsonOS_continuum_preHilbertData_isNormalized
    S D halfExtent N hN beta hbeta Q.toWeakStarBridge hInvariant

/-- Exact continuum vacuum-sector semigroup decay follows from scalar weak-star
quadratic convergence, with no finite/continuum Hilbert-space embedding. -/
theorem continuum_operator_vacuumOrthogonal_norm_le_exp_3320
    (A : PhysicalYangMillsEvenPeriodicWilsonOSRealizableFloorWeakStarQuadraticGapTransfer
      S D halfExtent N hN beta hbeta Q E R hInvariant B T)
    (t : NNReal)
    (psi :
      (physical_yang_mills_evenPeriodicWilsonOS_continuum_preHilbertData
        S D halfExtent N hN beta hbeta Q.toWeakStarBridge hInvariant).PhysicalHilbert)
    (hpsi : inner ℝ psi
      (physical_yang_mills_evenPeriodicWilsonOS_continuum_preHilbertData
        S D halfExtent N hN beta hbeta Q.toWeakStarBridge hInvariant).vacuum = 0) :
    ‖T.toPhysicalSemigroup.operator t psi‖ ≤
      Real.exp (-physicalYangMillsExact3320Mass * (t : ℝ)) * ‖psi‖ := by
  have h := A.toHalfQuadraticGapCertificate.norm_decay
    A.continuum_isNormalized t psi hpsi
  simpa only [sqrt_exp_neg_mul_double_nnreal] using h

/-- With strong continuity on the continuum observable core, the scalar
weak-star transfer yields the exact `33/20` right-Hamiltonian Rayleigh bound. -/
theorem rightHamiltonian_inner_ge_3320_mul_norm_sq
    (A : PhysicalYangMillsEvenPeriodicWilsonOSRealizableFloorWeakStarQuadraticGapTransfer
      S D halfExtent N hN beta hbeta Q E R hInvariant B T)
    (hContinuous : T.StrongContinuityOnObservableStates)
    (psi :
      (StrongContinuityOnObservableStates.toStronglyContinuousPhysicalSemigroup
        T hContinuous).rightGeneratorDomain)
    (hpsi : inner ℝ
      (psi : (physical_yang_mills_evenPeriodicWilsonOS_continuum_preHilbertData
        S D halfExtent N hN beta hbeta Q.toWeakStarBridge hInvariant).PhysicalHilbert)
      (physical_yang_mills_evenPeriodicWilsonOS_continuum_preHilbertData
        S D halfExtent N hN beta hbeta Q.toWeakStarBridge hInvariant).vacuum = 0) :
    (33 : ℝ) / 20 *
        ‖(psi : (physical_yang_mills_evenPeriodicWilsonOS_continuum_preHilbertData
          S D halfExtent N hN beta hbeta Q.toWeakStarBridge hInvariant).PhysicalHilbert)‖ ^ 2 ≤
      inner ℝ
        ((StrongContinuityOnObservableStates.toStronglyContinuousPhysicalSemigroup
          T hContinuous).rightHamiltonian psi)
        (psi : (physical_yang_mills_evenPeriodicWilsonOS_continuum_preHilbertData
          S D halfExtent N hN beta hbeta Q.toWeakStarBridge hInvariant).PhysicalHilbert) := by
  simpa [physicalYangMillsExact3320Mass] using
    A.toHalfQuadraticGapCertificate.rightHamiltonian_inner_ge_mass_mul_norm_sq
      A.continuum_isNormalized hContinuous psi hpsi

/-- The exact `33/20` lower bound also survives graph closure of the continuum
OS Hamiltonian. -/
theorem closedRightHamiltonian_inner_ge_3320_mul_norm_sq
    (A : PhysicalYangMillsEvenPeriodicWilsonOSRealizableFloorWeakStarQuadraticGapTransfer
      S D halfExtent N hN beta hbeta Q E R hInvariant B T)
    (hContinuous : T.StrongContinuityOnObservableStates)
    (psi :
      (StrongContinuityOnObservableStates.toStronglyContinuousPhysicalSemigroup
        T hContinuous).closedRightHamiltonian.domain)
    (hpsi : inner ℝ
      (psi : (physical_yang_mills_evenPeriodicWilsonOS_continuum_preHilbertData
        S D halfExtent N hN beta hbeta Q.toWeakStarBridge hInvariant).PhysicalHilbert)
      (physical_yang_mills_evenPeriodicWilsonOS_continuum_preHilbertData
        S D halfExtent N hN beta hbeta Q.toWeakStarBridge hInvariant).vacuum = 0) :
    (33 : ℝ) / 20 *
        ‖(psi : (physical_yang_mills_evenPeriodicWilsonOS_continuum_preHilbertData
          S D halfExtent N hN beta hbeta Q.toWeakStarBridge hInvariant).PhysicalHilbert)‖ ^ 2 ≤
      inner ℝ
        ((StrongContinuityOnObservableStates.toStronglyContinuousPhysicalSemigroup
          T hContinuous).closedRightHamiltonian psi)
        (psi : (physical_yang_mills_evenPeriodicWilsonOS_continuum_preHilbertData
          S D halfExtent N hN beta hbeta Q.toWeakStarBridge hInvariant).PhysicalHilbert) := by
  simpa [physicalYangMillsExact3320Mass] using
    A.toHalfQuadraticGapCertificate.closedRightHamiltonian_inner_ge_mass_mul_norm_sq
      A.continuum_isNormalized hContinuous psi hpsi

end PhysicalYangMillsEvenPeriodicWilsonOSRealizableFloorWeakStarQuadraticGapTransfer

end MathlibAnalytic
end MGAP4D

end