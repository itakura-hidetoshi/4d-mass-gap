# Complete construction direct bounded root smoke

This layer checks that the aggregate `MGAP4D.MathlibAnalytic` root import exposes the complete Yang--Mills direct bounded certificate surface.

It verifies root-level access to:

- `EuclideanYangMillsCompleteConstructionDirectBoundedCertificate`
- `euclideanYangMillsCompleteConstructionDirectBoundedCertificate`
- the full spectral package projection from the certificate surface
- the direct bounded public handoff status
- the primary direct bare-`M` boundedness route marker
- the compatibility-only route-backed marker
- the preferred direct bounded endpoint names

This is a downstream import-surface and compile-smoke layer.

It does not add a new spectral theorem, spectral projection construction, numerical mass-gap bound, or final Yang--Mills mass-gap claim.
