import MGAP4D.MathlibAnalytic.PhysicalYangMillsGaugeInvariantOSBilinearForm
import Mathlib.Analysis.InnerProductSpace.Completion

namespace MGAP4D
namespace MathlibAnalytic

noncomputable section

/-- A reflection-invariant, reflection-positive weak-star state, packaged as the
input from which the Osterwalder--Schrader pre-Hilbert space is generated. -/
structure PhysicalYangMillsGaugeInvariantOSReflectionData.OSPreHilbertData
    {S : PhysicalFourDimensionalYangMillsSymmetryLimit}
    (D : PhysicalYangMillsGaugeInvariantOSReflectionData S) where
  omega : WeakDual ℝ
    (physicalYangMillsGaugeInvariantObservableSubalgebra S)
  reflectionInvariant : D.WeakStarReflectionInvariant omega
  reflectionPositive : D.WeakStarReflectionPositive omega

/-- An opaque copy of the positive-time observable module.  Opacity is
essential here: bounded continuous observables already carry a supremum norm,
whereas the OS reconstruction requires the different seminorm generated by
`omega (Theta F * F)`. -/
def PhysicalYangMillsGaugeInvariantOSReflectionData.OSPreHilbertData.Carrier
    {S : PhysicalFourDimensionalYangMillsSymmetryLimit}
    {D : PhysicalYangMillsGaugeInvariantOSReflectionData S}
    (P : D.OSPreHilbertData) : Type :=
  D.positiveTimeSubalgebra.toSubmodule

noncomputable instance
    PhysicalYangMillsGaugeInvariantOSReflectionData.OSPreHilbertData.carrierAddCommGroup
    {S : PhysicalFourDimensionalYangMillsSymmetryLimit}
    {D : PhysicalYangMillsGaugeInvariantOSReflectionData S}
    (P : D.OSPreHilbertData) : AddCommGroup P.Carrier := by
  change AddCommGroup D.positiveTimeSubalgebra.toSubmodule
  infer_instance

noncomputable instance
    PhysicalYangMillsGaugeInvariantOSReflectionData.OSPreHilbertData.carrierModule
    {S : PhysicalFourDimensionalYangMillsSymmetryLimit}
    {D : PhysicalYangMillsGaugeInvariantOSReflectionData S}
    (P : D.OSPreHilbertData) : Module ℝ P.Carrier := by
  change Module ℝ D.positiveTimeSubalgebra.toSubmodule
  infer_instance

/-- The canonical linear identification with the original positive-time
observable module. -/
@[reducible] noncomputable def
    PhysicalYangMillsGaugeInvariantOSReflectionData.OSPreHilbertData.toPositiveTime
    {S : PhysicalFourDimensionalYangMillsSymmetryLimit}
    {D : PhysicalYangMillsGaugeInvariantOSReflectionData S}
    (P : D.OSPreHilbertData) :
    P.Carrier ≃ₗ[ℝ] D.positiveTimeSubalgebra.toSubmodule := by
  change D.positiveTimeSubalgebra.toSubmodule ≃ₗ[ℝ]
    D.positiveTimeSubalgebra.toSubmodule
  exact LinearEquiv.refl ℝ _

/-- The positive semidefinite OS form as a Mathlib pre-inner-product core. -/
noncomputable def
    PhysicalYangMillsGaugeInvariantOSReflectionData.OSPreHilbertData.core
    {S : PhysicalFourDimensionalYangMillsSymmetryLimit}
    {D : PhysicalYangMillsGaugeInvariantOSReflectionData S}
    (P : D.OSPreHilbertData) :
    PreInnerProductSpace.Core ℝ P.Carrier where
  inner F G := D.osBilinForm P.omega (P.toPositiveTime F) (P.toPositiveTime G)
  conj_inner_symm F G := by
    simpa using
      (D.osBilinForm_isSymm P.omega P.reflectionInvariant).eq
        (P.toPositiveTime G) (P.toPositiveTime F)
  re_inner_nonneg F := by
    simpa using
      (D.osBilinForm_isNonneg P.omega P.reflectionPositive).nonneg
        (P.toPositiveTime F)
  add_left F G H := by
    simp
  smul_left F G r := by
    simp

noncomputable instance
    PhysicalYangMillsGaugeInvariantOSReflectionData.OSPreHilbertData.carrierSeminormedAddCommGroup
    {S : PhysicalFourDimensionalYangMillsSymmetryLimit}
    {D : PhysicalYangMillsGaugeInvariantOSReflectionData S}
    (P : D.OSPreHilbertData) : SeminormedAddCommGroup P.Carrier := by
  letI : PreInnerProductSpace.Core ℝ P.Carrier := P.core
  exact InnerProductSpace.Core.toSeminormedAddCommGroup

noncomputable instance
    PhysicalYangMillsGaugeInvariantOSReflectionData.OSPreHilbertData.carrierNormedSpace
    {S : PhysicalFourDimensionalYangMillsSymmetryLimit}
    {D : PhysicalYangMillsGaugeInvariantOSReflectionData S}
    (P : D.OSPreHilbertData) : NormedSpace ℝ P.Carrier := by
  letI : PreInnerProductSpace.Core ℝ P.Carrier := P.core
  exact InnerProductSpace.Core.toNormedSpace

noncomputable instance
    PhysicalYangMillsGaugeInvariantOSReflectionData.OSPreHilbertData.carrierInnerProductSpace
    {S : PhysicalFourDimensionalYangMillsSymmetryLimit}
    {D : PhysicalYangMillsGaugeInvariantOSReflectionData S}
    (P : D.OSPreHilbertData) : InnerProductSpace ℝ P.Carrier := by
  exact InnerProductSpace.ofCore P.core

/-- The inner product on the opaque carrier is definitionally the reflected
continuum expectation. -/
@[simp] theorem
    PhysicalYangMillsGaugeInvariantOSReflectionData.OSPreHilbertData.inner_eq_osBilinForm
    {S : PhysicalFourDimensionalYangMillsSymmetryLimit}
    {D : PhysicalYangMillsGaugeInvariantOSReflectionData S}
    (P : D.OSPreHilbertData) (F G : P.Carrier) :
    inner ℝ F G =
      D.osBilinForm P.omega (P.toPositiveTime F) (P.toPositiveTime G) := by
  rfl

/-- The OS null vectors form a real linear subspace. -/
def PhysicalYangMillsGaugeInvariantOSReflectionData.OSPreHilbertData.nullSubmodule
    {S : PhysicalFourDimensionalYangMillsSymmetryLimit}
    {D : PhysicalYangMillsGaugeInvariantOSReflectionData S}
    (P : D.OSPreHilbertData) : Submodule ℝ P.Carrier where
  carrier := {F | ‖F‖ = 0}
  zero_mem' := norm_zero
  add_mem' := by
    intro F G hF hG
    apply le_antisymm
    · calc
        ‖F + G‖ ≤ ‖F‖ + ‖G‖ := norm_add_le F G
        _ = 0 := by rw [hF, hG]; simp
    · exact norm_nonneg _
  smul_mem' := by
    intro r F hF
    simp [hF]

@[simp] theorem
    PhysicalYangMillsGaugeInvariantOSReflectionData.OSPreHilbertData.mem_nullSubmodule
    {S : PhysicalFourDimensionalYangMillsSymmetryLimit}
    {D : PhysicalYangMillsGaugeInvariantOSReflectionData S}
    (P : D.OSPreHilbertData) (F : P.Carrier) :
    F ∈ P.nullSubmodule ↔ ‖F‖ = 0 :=
  Iff.rfl

/-- The OS null condition is exactly vanishing of the reflected quadratic
expectation. -/
theorem PhysicalYangMillsGaugeInvariantOSReflectionData.OSPreHilbertData.mem_nullSubmodule_iff_osQuadratic_eq_zero
    {S : PhysicalFourDimensionalYangMillsSymmetryLimit}
    {D : PhysicalYangMillsGaugeInvariantOSReflectionData S}
    (P : D.OSPreHilbertData) (F : P.Carrier) :
    F ∈ P.nullSubmodule ↔
      D.osBilinForm P.omega (P.toPositiveTime F) (P.toPositiveTime F) = 0 := by
  rw [P.mem_nullSubmodule]
  constructor
  · intro hF
    calc
      D.osBilinForm P.omega (P.toPositiveTime F) (P.toPositiveTime F) =
          inner ℝ F F := by rw [P.inner_eq_osBilinForm]
      _ = ‖F‖ ^ 2 := real_inner_self_eq_norm_sq F
      _ = 0 := by rw [hF]; simp
  · intro hF
    have hsq : ‖F‖ ^ 2 = 0 := by
      rw [← real_inner_self_eq_norm_sq F, P.inner_eq_osBilinForm]
      exact hF
    exact sq_eq_zero_iff.mp hsq

/-- The separated OS pre-Hilbert space.  Mathlib's `SeparationQuotient`
identifies precisely the vectors invisible to the OS seminorm. -/
abbrev PhysicalYangMillsGaugeInvariantOSReflectionData.OSPreHilbertData.Separated
    {S : PhysicalFourDimensionalYangMillsSymmetryLimit}
    {D : PhysicalYangMillsGaugeInvariantOSReflectionData S}
    (P : D.OSPreHilbertData) : Type :=
  SeparationQuotient P.Carrier

/-- The completed physical OS Hilbert carrier. -/
def PhysicalYangMillsGaugeInvariantOSReflectionData.OSPreHilbertData.PhysicalHilbert
    {S : PhysicalFourDimensionalYangMillsSymmetryLimit}
    {D : PhysicalYangMillsGaugeInvariantOSReflectionData S}
    (P : D.OSPreHilbertData) : Type :=
  UniformSpace.Completion P.Separated

noncomputable instance
    PhysicalYangMillsGaugeInvariantOSReflectionData.OSPreHilbertData.physicalHilbertNormedAddCommGroup
    {S : PhysicalFourDimensionalYangMillsSymmetryLimit}
    {D : PhysicalYangMillsGaugeInvariantOSReflectionData S}
    (P : D.OSPreHilbertData) : NormedAddCommGroup P.PhysicalHilbert := by
  change NormedAddCommGroup (UniformSpace.Completion P.Separated)
  exact UniformSpace.Completion.instNormedAddCommGroup P.Separated

noncomputable instance
    PhysicalYangMillsGaugeInvariantOSReflectionData.OSPreHilbertData.physicalHilbertInnerProductSpace
    {S : PhysicalFourDimensionalYangMillsSymmetryLimit}
    {D : PhysicalYangMillsGaugeInvariantOSReflectionData S}
    (P : D.OSPreHilbertData) : InnerProductSpace ℝ P.PhysicalHilbert := by
  change InnerProductSpace ℝ (UniformSpace.Completion P.Separated)
  exact UniformSpace.Completion.innerProductSpace

noncomputable instance
    PhysicalYangMillsGaugeInvariantOSReflectionData.OSPreHilbertData.physicalHilbertCompleteSpace
    {S : PhysicalFourDimensionalYangMillsSymmetryLimit}
    {D : PhysicalYangMillsGaugeInvariantOSReflectionData S}
    (P : D.OSPreHilbertData) : CompleteSpace P.PhysicalHilbert := by
  change CompleteSpace (UniformSpace.Completion P.Separated)
  exact UniformSpace.Completion.completeSpace P.Separated

/-- The OS class of a positive-time observable before Hilbert completion. -/
def PhysicalYangMillsGaugeInvariantOSReflectionData.OSPreHilbertData.osClass
    {S : PhysicalFourDimensionalYangMillsSymmetryLimit}
    {D : PhysicalYangMillsGaugeInvariantOSReflectionData S}
    (P : D.OSPreHilbertData) (F : P.Carrier) : P.Separated :=
  SeparationQuotient.mk F

/-- The dense physical vector represented by a positive-time observable. -/
def PhysicalYangMillsGaugeInvariantOSReflectionData.OSPreHilbertData.physicalState
    {S : PhysicalFourDimensionalYangMillsSymmetryLimit}
    {D : PhysicalYangMillsGaugeInvariantOSReflectionData S}
    (P : D.OSPreHilbertData) (F : P.Carrier) : P.PhysicalHilbert := by
  change UniformSpace.Completion P.Separated
  exact (P.osClass F : UniformSpace.Completion P.Separated)

/-- The separated OS quotient is dense in the completed physical carrier. -/
theorem PhysicalYangMillsGaugeInvariantOSReflectionData.OSPreHilbertData.separated_dense_in_physical
    {S : PhysicalFourDimensionalYangMillsSymmetryLimit}
    {D : PhysicalYangMillsGaugeInvariantOSReflectionData S}
    (P : D.OSPreHilbertData) :
    DenseRange
      (fun x : P.Separated =>
        (x : UniformSpace.Completion P.Separated)) :=
  UniformSpace.Completion.denseRange_coe

/-- The actual continuum even-periodic Wilson state supplies all data required
for the OS null quotient and Hilbert completion. -/
noncomputable def physical_yang_mills_evenPeriodicWilsonOS_continuum_preHilbertData
    (S : PhysicalFourDimensionalYangMillsSymmetryLimit)
    (D : PhysicalYangMillsGaugeInvariantOSReflectionData S)
    (halfExtent : ℕ → ℕ)
    (N : ℕ)
    (hN : 0 < N)
    [Nontrivial (Matrix.specialUnitaryGroup (Fin N) ℂ)]
    (beta : ℕ → ℝ)
    (hbeta : ∀ n, 0 ≤ beta n)
    (B : PhysicalYangMillsEvenPeriodicWilsonOSWeakStarBridge
      S D halfExtent N hN beta hbeta)
    (hInvariant : ∀ n,
      D.WeakStarReflectionInvariant
        (physicalYangMillsApproximatingGaugeInvariantWeakStarState S n)) :
    D.OSPreHilbertData :=
  { omega := physicalYangMillsContinuumGaugeInvariantWeakStarState S
    reflectionInvariant :=
      physical_yang_mills_gaugeInvariantWeakStarReflectionInvariance_passes_to_limit
        S D hInvariant
    reflectionPositive :=
      physical_yang_mills_evenPeriodicWilsonOS_continuum_weakStarReflectionPositive
        S D halfExtent N hN beta hbeta B }

/-- The actual continuum Wilson OS state therefore has a complete real Hilbert
space obtained canonically from positive-time gauge-invariant observables. -/
theorem physical_yang_mills_evenPeriodicWilsonOS_continuum_physicalHilbert_complete
    (S : PhysicalFourDimensionalYangMillsSymmetryLimit)
    (D : PhysicalYangMillsGaugeInvariantOSReflectionData S)
    (halfExtent : ℕ → ℕ)
    (N : ℕ)
    (hN : 0 < N)
    [Nontrivial (Matrix.specialUnitaryGroup (Fin N) ℂ)]
    (beta : ℕ → ℝ)
    (hbeta : ∀ n, 0 ≤ beta n)
    (B : PhysicalYangMillsEvenPeriodicWilsonOSWeakStarBridge
      S D halfExtent N hN beta hbeta)
    (hInvariant : ∀ n,
      D.WeakStarReflectionInvariant
        (physicalYangMillsApproximatingGaugeInvariantWeakStarState S n)) :
    CompleteSpace
      (physical_yang_mills_evenPeriodicWilsonOS_continuum_preHilbertData
        S D halfExtent N hN beta hbeta B hInvariant).PhysicalHilbert := by
  infer_instance

end

end MathlibAnalytic
end MGAP4D
