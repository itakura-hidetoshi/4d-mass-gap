import MGAP4D.MathlibAnalytic.PeriodicHypercubicEvenSpecialUnitaryPhysicalTransferContinuousVacuumReferenceFixedRightTargetRatioResponseControlledStationaryFiniteStep
import MGAP4D.MathlibAnalytic.PeriodicHypercubicEvenSpecialUnitaryPhysicalTransferContinuousVacuumReferenceFixedTargetPhysicalLeftExponentialWeightedPinAbsorption
import Mathlib.Tactic

/-!
# Response-controlled weighted random-scan orbit

This file turns a target-centered exponentially weighted column bound on the
fixed-right response profile itself into a weighted contraction estimate for
the response-controlled physical random-scan recurrence.

For
  W_T(x) = s ^ dist(T,x)
and a nonnegative response profile R, assume only the scalar weighted-column
bound
  sum_x R(x,y) W_T(x) <= M W_T(y).

The response-controlled physical kernel then has weighted column coefficient
  c_R = 18 * eta(beta) * s^2 + eta(beta) + exp(16 beta) * M.

Consequently one random-scan step has reciprocal rate
  q_R = |Lambda|^{-1} (|Lambda| - 1 + c_R),
and every nonnegative profile dominated by C W_T remains bounded after n steps
by
  q_R^n C W_T.

The unknown M is the weighted mass of the fixed-right response profile itself.
No remote kappa certificate, covariance decay, sweep contraction, coercivity,
or mass-gap input is assumed here.
-/

namespace MGAP4D
namespace MathlibAnalytic

open scoped BigOperators

noncomputable section

local instance responseControlledWeightedRandomScanOrbitSpatialLinkFintype
    (H : ℕ) : Fintype (PeriodicHypercubicEvenSpatialSliceLink H) :=
  Fintype.ofFinite _

/-- Uniform target-centered weighted-column control of a fixed-right response
profile. -/
def
    periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceFixedRightTargetRatioResponseExponentialWeightedColumnBound
    (H : ℕ)
    (s : ℝ)
    (distinguishedTarget : PeriodicHypercubicEvenSpatialSliceLink H)
    (R :
      PeriodicHypercubicEvenSpatialSliceLink H →
        PeriodicHypercubicEvenSpatialSliceLink H → ℝ)
    (responseCoefficient : ℝ) : Prop :=
  ∀ source : PeriodicHypercubicEvenSpatialSliceLink H,
    (∑ target : PeriodicHypercubicEvenSpatialSliceLink H,
      R target source *
        periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferencePhysicalLeftLocalHarnackBaseL1ExponentialWeight
          H s distinguishedTarget target) ≤
      responseCoefficient *
        periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferencePhysicalLeftLocalHarnackBaseL1ExponentialWeight
          H s distinguishedTarget source

/-- Weighted column coefficient generated by the local Harnack part, the
distinguished-target pin, and the weighted fixed-right response mass. -/
noncomputable def
    periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceResponseControlledExponentialWeightedColumnCoefficient
    (beta s responseCoefficient : ℝ) : ℝ :=
  18 *
      periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceBackgroundUpdateHarnackInfluence
        beta *
    s ^ 2 +
    periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceBackgroundUpdateHarnackInfluence
      beta +
    Real.exp (16 * beta) * responseCoefficient

theorem
    periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceResponseControlledExponentialWeightedColumnCoefficient_nonneg
    (beta s responseCoefficient : ℝ)
    (hbeta : 0 ≤ beta)
    (hResponseCoefficient : 0 ≤ responseCoefficient) :
    0 ≤
      periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceResponseControlledExponentialWeightedColumnCoefficient
        beta s responseCoefficient := by
  unfold
    periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceResponseControlledExponentialWeightedColumnCoefficient
  have hEta :
      0 ≤
        periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceBackgroundUpdateHarnackInfluence
          beta :=
    periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceBackgroundUpdateHarnackInfluence_nonneg
      beta hbeta
  positivity

/-- Pointwise decomposition of the response-controlled physical kernel into
the genuine local Harnack kernel, one distinguished-target pin, and the
linearized fixed-right response block. -/
theorem
    periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceFixedTargetResponseControlledPhysicalLeftKernel_le_local_add_targetPin_add_response
    (H : ℕ)
    (beta : ℝ)
    (hbeta : 0 ≤ beta)
    (distinguishedTarget target source :
      PeriodicHypercubicEvenSpatialSliceLink H)
    (R :
      PeriodicHypercubicEvenSpatialSliceLink H →
        PeriodicHypercubicEvenSpatialSliceLink H → ℝ)
    (hRNonneg : ∀ target source, 0 ≤ R target source) :
    (periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceFixedTargetResponseControlledPhysicalLeftKernel
        H beta hbeta distinguishedTarget R hRNonneg).influence target source ≤
      (periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferencePhysicalLeftLocalHarnackKernel
          H beta hbeta).influence target source +
        (if target = distinguishedTarget then
          periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceBackgroundUpdateHarnackInfluence
            beta
        else 0) +
        Real.exp (16 * beta) * R target source := by
  classical
  let eta :=
    periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceBackgroundUpdateHarnackInfluence
      beta
  have hEta : 0 ≤ eta := by
    exact
      periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceBackgroundUpdateHarnackInfluence_nonneg
        beta hbeta
  have hLocal :
      0 ≤
        (periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferencePhysicalLeftLocalHarnackKernel
          H beta hbeta).influence target source :=
    (periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferencePhysicalLeftLocalHarnackKernel
      H beta hbeta).influence_nonneg target source
  have hResponse : 0 ≤ Real.exp (16 * beta) * R target source :=
    mul_nonneg (Real.exp_pos _).le (hRNonneg target source)
  by_cases hEq : target = source
  · subst target
    rw [
      (periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceFixedTargetResponseControlledPhysicalLeftKernel
        H beta hbeta distinguishedTarget R hRNonneg).influence_diagonal_zero source]
    exact add_nonneg (add_nonneg hLocal (by split <;> positivity)) hResponse
  · by_cases hDist : target = distinguishedTarget
    · subst target
      have hExceptional :
          distinguishedTarget ∈
            periodicHypercubicEvenSpatialSliceC5ExceptionalBackgroundFibers
              H source distinguishedTarget := by
        simp [
          periodicHypercubicEvenSpatialSliceC5ExceptionalBackgroundFibers]
      have hKernelEq :
          (periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceFixedTargetResponseControlledPhysicalLeftKernel
              H beta hbeta distinguishedTarget R hRNonneg).influence
              distinguishedTarget source = eta := by
        simp [
          periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceFixedTargetResponseControlledPhysicalLeftKernel,
          hEq, hExceptional, eta]
      rw [hKernelEq, if_pos rfl]
      nlinarith
    · by_cases hActive :
        target ∈ periodicHypercubicEvenSpatialSliceActiveNeighbors H source
      · have hExceptional :
            target ∈
              periodicHypercubicEvenSpatialSliceC5ExceptionalBackgroundFibers
                H source distinguishedTarget := by
          simp [
            periodicHypercubicEvenSpatialSliceC5ExceptionalBackgroundFibers,
            hActive]
        have hLocalEq :
            (periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferencePhysicalLeftLocalHarnackKernel
                H beta hbeta).influence target source = eta := by
          simp only [
            periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferencePhysicalLeftLocalHarnackKernel]
          rw [if_pos hActive]
        simp only [
          periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceFixedTargetResponseControlledPhysicalLeftKernel,
          hEq, if_false, hExceptional, if_true, hDist, hLocalEq]
        nlinarith
      · have hNotExceptional :
            target ∉
              periodicHypercubicEvenSpatialSliceC5ExceptionalBackgroundFibers
                H source distinguishedTarget := by
          simp [
            periodicHypercubicEvenSpatialSliceC5ExceptionalBackgroundFibers,
            hEq, hDist, hActive]
        have hKernelEq :
            (periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceFixedTargetResponseControlledPhysicalLeftKernel
                H beta hbeta distinguishedTarget R hRNonneg).influence target source =
              Real.exp (16 * beta) * R target source := by
          simp [
            periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceFixedTargetResponseControlledPhysicalLeftKernel,
            hEq, hNotExceptional]
        rw [hKernelEq, if_neg hDist]
        nlinarith

/-- The response-controlled physical kernel inherits a volume-independent
weighted column bound from the weighted mass of the response profile itself. -/
theorem
    periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceFixedTargetResponseControlledPhysicalLeftKernel_exponentialWeightedColumn_le
    (H : ℕ)
    (beta : ℝ)
    (hbeta : 0 ≤ beta)
    (s : ℝ)
    (hs : 1 ≤ s)
    (distinguishedTarget source :
      PeriodicHypercubicEvenSpatialSliceLink H)
    (R :
      PeriodicHypercubicEvenSpatialSliceLink H →
        PeriodicHypercubicEvenSpatialSliceLink H → ℝ)
    (hRNonneg : ∀ target source, 0 ≤ R target source)
    (responseCoefficient : ℝ)
    (hResponseWeighted :
      periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceFixedRightTargetRatioResponseExponentialWeightedColumnBound
        H s distinguishedTarget R responseCoefficient) :
    (∑ target : PeriodicHypercubicEvenSpatialSliceLink H,
      (periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceFixedTargetResponseControlledPhysicalLeftKernel
          H beta hbeta distinguishedTarget R hRNonneg).influence target source *
        periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferencePhysicalLeftLocalHarnackBaseL1ExponentialWeight
          H s distinguishedTarget target) ≤
      periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceResponseControlledExponentialWeightedColumnCoefficient
          beta s responseCoefficient *
        periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferencePhysicalLeftLocalHarnackBaseL1ExponentialWeight
          H s distinguishedTarget source := by
  classical
  let eta :=
    periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceBackgroundUpdateHarnackInfluence
      beta
  let W :=
    periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferencePhysicalLeftLocalHarnackBaseL1ExponentialWeight
      H s distinguishedTarget
  have hsNonneg : 0 ≤ s := le_trans (by norm_num) hs
  have hWeightNonneg : ∀ target, 0 ≤ W target := by
    intro target
    exact
      periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferencePhysicalLeftLocalHarnackBaseL1ExponentialWeight_nonneg
        H s hsNonneg distinguishedTarget target
  have hPointwise :
      ∀ target : PeriodicHypercubicEvenSpatialSliceLink H,
        (periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceFixedTargetResponseControlledPhysicalLeftKernel
            H beta hbeta distinguishedTarget R hRNonneg).influence target source *
            W target ≤
          ((periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferencePhysicalLeftLocalHarnackKernel
              H beta hbeta).influence target source +
            (if target = distinguishedTarget then eta else 0) +
            Real.exp (16 * beta) * R target source) * W target := by
    intro target
    exact mul_le_mul_of_nonneg_right
      (by
        simpa [eta] using
          periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceFixedTargetResponseControlledPhysicalLeftKernel_le_local_add_targetPin_add_response
            H beta hbeta distinguishedTarget target source R hRNonneg)
      (hWeightNonneg target)
  have hLocal :=
    periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferencePhysicalLeftLocalHarnackKernel_exponentialWeightedColumnSum_le
      H beta hbeta s hs distinguishedTarget source
  have hPin :=
    periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferencePhysicalLeftFixedTargetHarnackPin_exponentialWeighted_le_sourceWeight
      H beta hbeta s hs distinguishedTarget source
  have hResponse := hResponseWeighted source
  have hExpNonneg : 0 ≤ Real.exp (16 * beta) := (Real.exp_pos _).le
  calc
    (∑ target : PeriodicHypercubicEvenSpatialSliceLink H,
      (periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceFixedTargetResponseControlledPhysicalLeftKernel
          H beta hbeta distinguishedTarget R hRNonneg).influence target source *
        W target) ≤
      ∑ target : PeriodicHypercubicEvenSpatialSliceLink H,
        ((periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferencePhysicalLeftLocalHarnackKernel
            H beta hbeta).influence target source +
          (if target = distinguishedTarget then eta else 0) +
          Real.exp (16 * beta) * R target source) * W target := by
        exact Finset.sum_le_sum fun target _ => hPointwise target
    _ =
      (∑ target : PeriodicHypercubicEvenSpatialSliceLink H,
        (periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferencePhysicalLeftLocalHarnackKernel
          H beta hbeta).influence target source * W target) +
      eta * W distinguishedTarget +
      Real.exp (16 * beta) *
        (∑ target : PeriodicHypercubicEvenSpatialSliceLink H,
          R target source * W target) := by
        simp_rw [add_mul]
        rw [Finset.sum_add_distrib, Finset.sum_add_distrib]
        have hPinSum :
            (∑ target : PeriodicHypercubicEvenSpatialSliceLink H,
              (if target = distinguishedTarget then eta else 0) * W target) =
              eta * W distinguishedTarget := by
          simp
        have hRemoteSum :
            (∑ target : PeriodicHypercubicEvenSpatialSliceLink H,
              Real.exp (16 * beta) * R target source * W target) =
              Real.exp (16 * beta) *
                (∑ target : PeriodicHypercubicEvenSpatialSliceLink H,
                  R target source * W target) := by
          rw [Finset.mul_sum]
          apply Finset.sum_congr rfl
          intro target _hTarget
          ring
        rw [hPinSum, hRemoteSum]
    _ ≤
      (18 *
          periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceBackgroundUpdateHarnackInfluence
            beta *
        s ^ 2) * W source +
      eta * W source +
      Real.exp (16 * beta) * (responseCoefficient * W source) := by
        exact add_le_add
          (add_le_add hLocal (by simpa [eta, W] using hPin))
          (mul_le_mul_of_nonneg_left hResponse hExpNonneg)
    _ =
      periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceResponseControlledExponentialWeightedColumnCoefficient
          beta s responseCoefficient * W source := by
        simp only [
          periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceResponseControlledExponentialWeightedColumnCoefficient]
        ring

/-- One response-controlled random-scan step preserves a target-centered
weighted bound with the reciprocal rate generated by the weighted response
column coefficient. -/
theorem
    periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceFixedTargetResponseControlledRandomScanUpdatedVariation_le_weightedRate_mul
    (H : ℕ)
    (beta : ℝ)
    (hbeta : 0 ≤ beta)
    (s : ℝ)
    (hs : 1 ≤ s)
    (distinguishedTarget :
      PeriodicHypercubicEvenSpatialSliceLink H)
    (R :
      PeriodicHypercubicEvenSpatialSliceLink H →
        PeriodicHypercubicEvenSpatialSliceLink H → ℝ)
    (hRNonneg : ∀ target source, 0 ≤ R target source)
    (responseCoefficient : ℝ)
    (hResponseCoefficient : 0 ≤ responseCoefficient)
    (hResponseWeighted :
      periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceFixedRightTargetRatioResponseExponentialWeightedColumnBound
        H s distinguishedTarget R responseCoefficient)
    (variation : PeriodicHypercubicEvenSpatialSliceLink H → ℝ)
    (hVariationNonneg : ∀ e, 0 ≤ variation e)
    (bound : ℝ)
    (hBoundNonneg : 0 ≤ bound)
    (hVariationBound :
      ∀ e : PeriodicHypercubicEvenSpatialSliceLink H,
        variation e ≤
          bound *
            periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferencePhysicalLeftLocalHarnackBaseL1ExponentialWeight
              H s distinguishedTarget e)
    (source : PeriodicHypercubicEvenSpatialSliceLink H) :
    periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceFixedTargetResponseControlledRandomScanUpdatedVariation
        H beta hbeta distinguishedTarget R hRNonneg variation source ≤
      finiteInfluenceKernelReciprocalRandomScanRate
        (PeriodicHypercubicEvenSpatialSliceLink H)
        (periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceResponseControlledExponentialWeightedColumnCoefficient
          beta s responseCoefficient) *
      bound *
      periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferencePhysicalLeftLocalHarnackBaseL1ExponentialWeight
        H s distinguishedTarget source := by
  classical
  let K :=
    periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceFixedTargetResponseControlledPhysicalLeftKernel
      H beta hbeta distinguishedTarget R hRNonneg
  let W :=
    periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferencePhysicalLeftLocalHarnackBaseL1ExponentialWeight
      H s distinguishedTarget
  let coefficient :=
    periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceResponseControlledExponentialWeightedColumnCoefficient
      beta s responseCoefficient
  let n : ℝ := Fintype.card (PeriodicHypercubicEvenSpatialSliceLink H)
  have hCardNat :
      0 < Fintype.card (PeriodicHypercubicEvenSpatialSliceLink H) :=
    Fintype.card_pos_iff.mpr ⟨distinguishedTarget⟩
  have hn : 0 < n := by
    dsimp [n]
    exact_mod_cast hCardNat
  have hnOne : 1 ≤ n := by
    dsimp [n]
    exact_mod_cast hCardNat
  have hColumn :
      (∑ target : PeriodicHypercubicEvenSpatialSliceLink H,
        K.influence target source * W target) ≤
        coefficient * W source := by
    simpa [K, W, coefficient] using
      periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceFixedTargetResponseControlledPhysicalLeftKernel_exponentialWeightedColumn_le
        H beta hbeta s hs distinguishedTarget source R hRNonneg
        responseCoefficient hResponseWeighted
  have hWeighted :
      (∑ target : PeriodicHypercubicEvenSpatialSliceLink H,
        K.influence target source * variation target) ≤
        coefficient * bound * W source := by
    calc
      (∑ target : PeriodicHypercubicEvenSpatialSliceLink H,
        K.influence target source * variation target) ≤
        ∑ target : PeriodicHypercubicEvenSpatialSliceLink H,
          K.influence target source * (bound * W target) := by
            apply Finset.sum_le_sum
            intro target _hTarget
            exact mul_le_mul_of_nonneg_left
              (by simpa [W] using hVariationBound target)
              (K.influence_nonneg target source)
      _ = bound *
          (∑ target : PeriodicHypercubicEvenSpatialSliceLink H,
            K.influence target source * W target) := by
            rw [Finset.mul_sum]
            apply Finset.sum_congr rfl
            intro target _hTarget
            ring
      _ ≤ bound * (coefficient * W source) :=
        mul_le_mul_of_nonneg_left hColumn hBoundNonneg
      _ = coefficient * bound * W source := by ring
  have hDirect :
      n * variation source - variation source ≤
        (n - 1) * (bound * W source) := by
    have hScale :=
      mul_le_mul_of_nonneg_left
        (by simpa [W] using hVariationBound source)
        (by linarith : 0 ≤ n - 1)
    nlinarith
  have hTargetSum :
      (∑ target : PeriodicHypercubicEvenSpatialSliceLink H,
        finiteInfluenceKernelUpdatedVariation K variation target source) ≤
        (n - 1 + coefficient) * bound * W source := by
    rw [finiteInfluenceKernelUpdatedVariation_sum_target_eq]
    change n * variation source +
        (∑ target : PeriodicHypercubicEvenSpatialSliceLink H,
          K.influence target source * variation target) -
        variation source ≤ _
    nlinarith
  unfold
    periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceFixedTargetResponseControlledRandomScanUpdatedVariation
    finiteInfluenceKernelRestrictedTargetRandomScanUpdatedVariation
    finiteInfluenceKernelReciprocalRandomScanRate
  change n⁻¹ *
      (∑ target : PeriodicHypercubicEvenSpatialSliceLink H,
        finiteInfluenceKernelUpdatedVariation K variation target source) ≤
      n⁻¹ * (n - 1 + coefficient) * bound * W source
  simpa [mul_assoc] using
    (mul_le_mul_of_nonneg_left hTargetSum (le_of_lt (inv_pos.mpr hn)))

/-- The complete response-controlled random-scan orbit obeys the corresponding
target-centered geometric weighted bound. -/
theorem
    periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceFixedTargetResponseControlledRandomScanVariationIterate_le_weightedRate_pow_mul
    (H : ℕ)
    (beta : ℝ)
    (hbeta : 0 ≤ beta)
    (s : ℝ)
    (hs : 1 ≤ s)
    (distinguishedTarget :
      PeriodicHypercubicEvenSpatialSliceLink H)
    (R :
      PeriodicHypercubicEvenSpatialSliceLink H →
        PeriodicHypercubicEvenSpatialSliceLink H → ℝ)
    (hRNonneg : ∀ target source, 0 ≤ R target source)
    (responseCoefficient : ℝ)
    (hResponseCoefficient : 0 ≤ responseCoefficient)
    (hResponseWeighted :
      periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceFixedRightTargetRatioResponseExponentialWeightedColumnBound
        H s distinguishedTarget R responseCoefficient)
    (variation : PeriodicHypercubicEvenSpatialSliceLink H → ℝ)
    (hVariationNonneg : ∀ e, 0 ≤ variation e)
    (bound : ℝ)
    (hBoundNonneg : 0 ≤ bound)
    (hVariationBound :
      ∀ e : PeriodicHypercubicEvenSpatialSliceLink H,
        variation e ≤
          bound *
            periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferencePhysicalLeftLocalHarnackBaseL1ExponentialWeight
              H s distinguishedTarget e)
    (n : ℕ)
    (source : PeriodicHypercubicEvenSpatialSliceLink H) :
    periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceFixedTargetResponseControlledRandomScanVariationIterate
        H beta hbeta distinguishedTarget R hRNonneg variation n source ≤
      (finiteInfluenceKernelReciprocalRandomScanRate
        (PeriodicHypercubicEvenSpatialSliceLink H)
        (periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceResponseControlledExponentialWeightedColumnCoefficient
          beta s responseCoefficient)) ^ n *
      bound *
      periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferencePhysicalLeftLocalHarnackBaseL1ExponentialWeight
        H s distinguishedTarget source := by
  let rate :=
    finiteInfluenceKernelReciprocalRandomScanRate
      (PeriodicHypercubicEvenSpatialSliceLink H)
      (periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceResponseControlledExponentialWeightedColumnCoefficient
        beta s responseCoefficient)
  have hCardNat :
      0 < Fintype.card (PeriodicHypercubicEvenSpatialSliceLink H) :=
    Fintype.card_pos_iff.mpr ⟨distinguishedTarget⟩
  have hCoefficientNonneg :
      0 ≤
        periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceResponseControlledExponentialWeightedColumnCoefficient
          beta s responseCoefficient :=
    periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceResponseControlledExponentialWeightedColumnCoefficient_nonneg
      beta s responseCoefficient hbeta hResponseCoefficient
  have hRateNonneg : 0 ≤ rate := by
    exact
      finiteInfluenceKernelReciprocalRandomScanRate_nonneg
        hCardNat
        (periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceResponseControlledExponentialWeightedColumnCoefficient
          beta s responseCoefficient)
        hCoefficientNonneg
  induction n generalizing source with
  | zero =>
      simpa [rate] using hVariationBound source
  | succ n ih =>
      rw [
        periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceFixedTargetResponseControlledRandomScanVariationIterate_succ]
      have hIterNonneg :
          ∀ e : PeriodicHypercubicEvenSpatialSliceLink H,
            0 ≤
              periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceFixedTargetResponseControlledRandomScanVariationIterate
                H beta hbeta distinguishedTarget R hRNonneg variation n e := by
        intro e
        exact
          periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceFixedTargetResponseControlledRandomScanVariationIterate_nonneg
            H beta hbeta distinguishedTarget R hRNonneg variation
            hVariationNonneg n e
      have hStep :=
        periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceFixedTargetResponseControlledRandomScanUpdatedVariation_le_weightedRate_mul
          H beta hbeta s hs distinguishedTarget R hRNonneg
          responseCoefficient hResponseCoefficient hResponseWeighted
          (periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceFixedTargetResponseControlledRandomScanVariationIterate
            H beta hbeta distinguishedTarget R hRNonneg variation n)
          hIterNonneg
          (rate ^ n * bound)
          (mul_nonneg (pow_nonneg hRateNonneg n) hBoundNonneg)
          (fun e => by simpa [rate, mul_assoc] using ih e)
          source
      calc
        periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceFixedTargetResponseControlledRandomScanUpdatedVariation
            H beta hbeta distinguishedTarget R hRNonneg
            (periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferenceFixedTargetResponseControlledRandomScanVariationIterate
              H beta hbeta distinguishedTarget R hRNonneg variation n)
            source ≤
          rate * (rate ^ n * bound) *
            periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferencePhysicalLeftLocalHarnackBaseL1ExponentialWeight
              H s distinguishedTarget source := by
                simpa [rate, mul_assoc] using hStep
        _ =
          rate ^ (n + 1) * bound *
            periodicHypercubicEvenSpecialUnitaryPhysicalOneSlabContinuousVacuumReferencePhysicalLeftLocalHarnackBaseL1ExponentialWeight
              H s distinguishedTarget source := by
                rw [pow_succ]
                ring

end

end MathlibAnalytic
end MGAP4D
