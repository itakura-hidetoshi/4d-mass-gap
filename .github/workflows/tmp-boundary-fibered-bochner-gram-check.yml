name: Temporary Boundary-Fibered Bochner Gram Check

on:
  pull_request:

concurrency:
  group: temporary-boundary-fibered-bochner-gram-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  boundary-fibered-bochner-gram:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
        with:
          show-progress: false
      - uses: actions/cache/restore@v5
        with:
          path: |
            .lake
            lake-manifest.json
          key: ${{ runner.os }}-lean-fast-lake-v5-${{ hashFiles('lean-toolchain', 'lakefile.lean', 'lake-manifest.json') }}-${{ github.event.pull_request.base.sha }}
          restore-keys: |
            ${{ runner.os }}-lean-fast-lake-v5-${{ hashFiles('lean-toolchain', 'lakefile.lean', 'lake-manifest.json') }}-
      - name: Install Lean
        run: |
          curl -sSfL https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh | sh -s -- -y --default-toolchain none
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"
      - name: Build boundary-fibered Bochner Gram bridge
        shell: bash
        run: |
          set +e
          lake build MGAP4D.MathlibAnalytic.PhysicalYangMillsOrientedWilsonOSBoundaryFiberedBochnerGram > /tmp/boundary-fibered-bochner.log 2>&1
          status=$?
          echo "$status" > /tmp/boundary-fibered-bochner.status
          if [ "$status" -eq 0 ]; then
            echo "Boundary-fibered Bochner Gram build passed" | tee /tmp/boundary-fibered-bochner-diagnostics.txt
          else
            {
              echo "Boundary-fibered Bochner Gram build failed; compact diagnostics follow"
              grep -n -B 4 -A 24 'error:' /tmp/boundary-fibered-bochner.log || tail -n 200 /tmp/boundary-fibered-bochner.log
            } | tee /tmp/boundary-fibered-bochner-diagnostics.txt
          fi
          exit 0
      - name: Upload diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: boundary-fibered-bochner-gram-diagnostics
          path: /tmp/boundary-fibered-bochner-diagnostics.txt
          if-no-files-found: error
      - name: Propagate build status
        if: always()
        shell: bash
        run: exit "$(cat /tmp/boundary-fibered-bochner.status)"
