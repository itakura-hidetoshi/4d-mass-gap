name: Temporary Feature Measurability Check

on:
  pull_request:

concurrency:
  group: temporary-feature-measurability-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  feature-measurability:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
        with:
          show-progress: false
      - uses: actions/cache/restore@v5
        with:
          path: |
            .lake
            lake-manifest.json
          key: ${{ runner.os }}-lean-fast-lake-v5-${{ hashFiles('lean-toolchain', 'lakefile.lean', 'lake-manifest.json') }}-${{ github.event.pull_request.base.sha }}
          restore-keys: |
            ${{ runner.os }}-lean-fast-lake-v5-${{ hashFiles('lean-toolchain', 'lakefile.lean', 'lake-manifest.json') }}-
      - name: Install Lean
        run: |
          curl -sSfL https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh | sh -s -- -y --default-toolchain none
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"
      - name: Build feature measurability
        shell: bash
        run: |
          set +e
          lake build MGAP4D.MathlibAnalytic.RealHilbertKernelFeatureMeasurability > /tmp/feature-measurability.log 2>&1
          status=$?
          echo "$status" > /tmp/feature-measurability.status
          if [ "$status" -eq 0 ]; then
            echo "Feature measurability build passed" | tee /tmp/feature-measurability-diagnostics.txt
          else
            {
              echo "Feature measurability build failed; compact diagnostics follow"
              grep -n -B 4 -A 18 'error:' /tmp/feature-measurability.log || tail -n 160 /tmp/feature-measurability.log
            } | tee /tmp/feature-measurability-diagnostics.txt
          fi
          exit 0
      - name: Upload diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: feature-measurability-diagnostics
          path: /tmp/feature-measurability-diagnostics.txt
          if-no-files-found: error
      - name: Propagate build status
        if: always()
        shell: bash
        run: exit "$(cat /tmp/feature-measurability.status)"
