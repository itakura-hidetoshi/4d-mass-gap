name: Retired Boundary-Fibered Product Measure Preflight

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This temporary workflow has been retired."
