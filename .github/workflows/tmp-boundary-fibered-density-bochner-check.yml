name: Temporary Boundary-Fibered Density Bochner Check

on:
  workflow_dispatch:
  pull_request:
    paths:
      - ".github/workflows/tmp-boundary-fibered-density-bochner-check.yml"
      - "MGAP4D/MathlibAnalytic/FiniteInvolutiveEdgeBoundaryFiberedDensityBochnerFactorization.lean"
      - "MGAP4D/MathlibAnalytic/FiniteInvolutiveEdgeBoundaryFiberedCoordinates.lean"
      - "MGAP4D/MathlibAnalytic/PhysicalYangMillsOrientedWilsonOSBochnerGram.lean"
      - "scripts/audit_changed_lean_preflight.py"
      - "scripts/audit_lean_source_integrity.py"
      - "lean-toolchain"
      - "lakefile.lean"
      - "lake-manifest.json"

permissions:
  contents: read

env:
  MATHLIB_NO_CACHE_ON_UPDATE: "1"
  LAKE_CACHE_REF_SHA: ${{ github.event_name == 'pull_request' && github.event.pull_request.base.sha || github.sha }}

jobs:
  density-bochner:
    name: Boundary-fibered density Bochner
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 2

      - name: Audit target source
        shell: bash
        run: |
          set -o pipefail
          python3 scripts/audit_lean_source_integrity.py \
            MGAP4D/MathlibAnalytic/FiniteInvolutiveEdgeBoundaryFiberedDensityBochnerFactorization.lean \
            2>&1 | tee /tmp/boundary-fibered-density-bochner-preflight.log
          python3 scripts/audit_changed_lean_preflight.py \
            MGAP4D/MathlibAnalytic/FiniteInvolutiveEdgeBoundaryFiberedDensityBochnerFactorization.lean \
            2>&1 | tee -a /tmp/boundary-fibered-density-bochner-preflight.log

      - name: Cache elan toolchain
        uses: actions/cache@v5
        with:
          path: ~/.elan
          key: ${{ runner.os }}-elan-${{ hashFiles('lean-toolchain') }}
          restore-keys: |
            ${{ runner.os }}-elan-

      - name: Restore Lake build cache
        uses: actions/cache/restore@v5
        with:
          path: |
            .lake
            lake-manifest.json
          key: ${{ runner.os }}-lean-fast-lake-v5-${{ hashFiles('lean-toolchain', 'lakefile.lean', 'lake-manifest.json') }}-${{ env.LAKE_CACHE_REF_SHA }}
          restore-keys: |
            ${{ runner.os }}-lean-fast-lake-v5-${{ hashFiles('lean-toolchain', 'lakefile.lean', 'lake-manifest.json') }}-
            ${{ runner.os }}-lean-fast-lake-v4-
            ${{ runner.os }}-lean-fast-v3-
            ${{ runner.os }}-lean-fast-
            ${{ runner.os }}-lean-

      - name: Install elan and Lean toolchain
        shell: bash
        run: |
          set -euxo pipefail
          if ! command -v elan >/dev/null 2>&1; then
            curl --retry 5 --retry-delay 10 --retry-all-errors -sSfL https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh | sh -s -- -y --default-toolchain none
          fi
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"

      - name: Build density-weighted Bochner factorization
        shell: bash
        run: |
          set -o pipefail
          lake build MGAP4D.MathlibAnalytic.FiniteInvolutiveEdgeBoundaryFiberedDensityBochnerFactorization 2>&1 | tee /tmp/boundary-fibered-density-bochner.log

      - name: Upload diagnostics
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: boundary-fibered-density-bochner-diagnostics
          path: |
            /tmp/boundary-fibered-density-bochner-preflight.log
            /tmp/boundary-fibered-density-bochner.log
          if-no-files-found: ignore
