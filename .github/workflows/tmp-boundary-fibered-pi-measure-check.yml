name: Temporary Boundary-Fibered Pi Measure Check

on:
  pull_request:

concurrency:
  group: temporary-boundary-fibered-pi-measure-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  boundary-fibered-pi-measure:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
        with:
          show-progress: false
      - uses: actions/cache/restore@v5
        with:
          path: |
            .lake
            lake-manifest.json
          key: ${{ runner.os }}-lean-fast-lake-v5-${{ hashFiles('lean-toolchain', 'lakefile.lean', 'lake-manifest.json') }}-${{ github.event.pull_request.base.sha }}
          restore-keys: |
            ${{ runner.os }}-lean-fast-lake-v5-${{ hashFiles('lean-toolchain', 'lakefile.lean', 'lake-manifest.json') }}-
      - name: Install Lean
        run: |
          curl -sSfL https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh | sh -s -- -y --default-toolchain none
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"
      - name: Build boundary-fibered pi measure factorization
        shell: bash
        run: |
          set +e
          lake build MGAP4D.MathlibAnalytic.FiniteInvolutiveEdgeBoundaryFiberedPiMeasure > /tmp/boundary-fibered-pi-measure.log 2>&1
          status=$?
          echo "$status" > /tmp/boundary-fibered-pi-measure.status
          if [ "$status" -eq 0 ]; then
            echo "Boundary-fibered pi measure build passed" | tee /tmp/boundary-fibered-pi-measure-diagnostics.txt
          else
            {
              echo "Boundary-fibered pi measure build failed; compact diagnostics follow"
              grep -n -B 4 -A 32 'error:' /tmp/boundary-fibered-pi-measure.log || tail -n 260 /tmp/boundary-fibered-pi-measure.log
            } | tee /tmp/boundary-fibered-pi-measure-diagnostics.txt
          fi
          exit 0
      - name: Upload diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: boundary-fibered-pi-measure-diagnostics
          path: /tmp/boundary-fibered-pi-measure-diagnostics.txt
          if-no-files-found: error
      - name: Propagate build status
        if: always()
        shell: bash
        run: exit "$(cat /tmp/boundary-fibered-pi-measure.status)"
