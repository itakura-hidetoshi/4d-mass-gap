name: Periodic Other Edge Uniqueness Diagnostic

on:
  pull_request:
    paths:
      - ".github/workflows/periodic-other-edge-uniqueness-diagnostic.yml"
      - "MGAP4D/MathlibAnalytic/PeriodicHypercubicOtherEdgeUniqueness.lean"

permissions:
  contents: read

env:
  MATHLIB_NO_CACHE_ON_UPDATE: "1"

jobs:
  diagnostic:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 2
      - uses: actions/cache/restore@v5
        with:
          path: |
            .lake
            lake-manifest.json
          key: ${{ runner.os }}-lean-fast-lake-v5-${{ hashFiles('lean-toolchain', 'lakefile.lean', 'lake-manifest.json') }}-${{ github.event.pull_request.base.sha }}
          restore-keys: |
            ${{ runner.os }}-lean-fast-lake-v5-${{ hashFiles('lean-toolchain', 'lakefile.lean', 'lake-manifest.json') }}-
            ${{ runner.os }}-lean-fast-
      - name: Install elan
        run: |
          if ! command -v elan >/dev/null 2>&1; then
            curl --retry 5 --retry-delay 10 --retry-all-errors -sSfL https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh | sh -s -- -y --default-toolchain none
          fi
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"
      - name: Build other-edge uniqueness
        id: build
        shell: bash
        run: |
          set +e
          lake build MGAP4D.MathlibAnalytic.PeriodicHypercubicOtherEdgeUniqueness > periodic-other-edge-uniqueness-full.log 2>&1
          code=$?
          tail -n 320 periodic-other-edge-uniqueness-full.log > periodic-other-edge-uniqueness-tail.log
          python3 scripts/lean_feedback_classifier.py --exit-code "$code" --pretty periodic-other-edge-uniqueness-full.log > periodic-other-edge-uniqueness-feedback.json
          cat periodic-other-edge-uniqueness-tail.log
          cat periodic-other-edge-uniqueness-feedback.json
          echo "exit_code=$code" >> "$GITHUB_OUTPUT"
      - name: Upload diagnostic
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: periodic-other-edge-uniqueness-diagnostic
          path: |
            periodic-other-edge-uniqueness-tail.log
            periodic-other-edge-uniqueness-feedback.json
      - name: Propagate failure
        if: steps.build.outputs.exit_code != '0'
        run: exit 1
